# Rule reference

> **This file is auto-generated by `scripts/generate_docs.py`.** Edit the YAML in `data/rules/` (and its implementation in `src/finops_assess/rules_impl/`), then re-run the script.

Counts: **26 rules** across 4 surfaces; **26 have a registered implementation**.

Severity values: `info` / `low` / `medium` / `high`.

## Microsoft 365 (`m365`)

| Rule ID | Severity | Impl | Inactivity (d) | Summary | Recommendation |
|---------|----------|------|----------------|---------|-----------------|
| `M365.COPILOT_INACTIVE_60D` | high | ✅ | 60 | Copilot for M365 add-on with no Copilot activity in 60 days. | {principal} has Copilot for M365 assigned but no Copilot interactions in the last 60 days. Reassign the seat. |
| `M365.DISABLED_USER_LICENSED` | high | ✅ | n/a | Disabled account still carries paid licenses. | {principal} (accountEnabled = false) still has {current_sku} assigned. Reclaim the license. |
| `M365.DUPLICATE_BUNDLE` | medium | ✅ | n/a | User holds both a bundle SKU and a child SKU it already includes. | {principal} has {current_sku}, which already covers the capabilities of {duplicate_sku}. Remove {duplicate_sku}. |
| `M365.E5_FEATURES_UNUSED` | medium | ✅ | 90 | E5 user shows no Defender / Purview / Entra-P2 signal in 90 days. | {principal} is on E5 but none of the E5-exclusive capabilities (Defender for O365 P2, Purview DLP/IP, Entra P2 risk policies) have been exercised in 90 days. Consider stepping down to E3 plus targeted add-ons. |
| `M365.GUEST_PREMIUM_LICENSED` | medium | ✅ | n/a | Entra B2B guest carries an interactive M365 license. | Guest user {principal} has {current_sku} assigned. Guests should rely on the host tenant's resources; remove the assignment unless explicitly justified. |
| `M365.OVER_LICENSED_VS_PERSONA` | medium | ✅ | n/a | Assigned SKU exceeds the features required by the user's persona. | {principal} is classified as persona '{persona}'. Their current SKU {current_sku} provides features beyond what this persona requires. Consider {recommended_sku}. |
| `M365.SHARED_MAILBOX_LICENSED` | medium | ✅ | n/a | Shared mailbox under 50 GB has a paid user license. | Shared mailbox {principal} ({mailbox_size_gb} GB) does not need a paid Exchange license below the 50 GB threshold. Remove {current_sku}. |
| `M365.UNUSED_LICENSE_30D` | high | ✅ | 30 | Assigned SKU shows no service-plan activity in 30 days. | Remove the {current_sku} assignment from {principal} or step down to a lower-cost SKU. No covered service has recorded activity in the last 30 days. |

## Azure (`azure`)

| Rule ID | Severity | Impl | Inactivity (d) | Summary | Recommendation |
|---------|----------|------|----------------|---------|-----------------|
| `AZ.COMMITMENT_RENEWAL_REVIEW` | medium | ✅ | 60 | Reservation expiring within the near-expiry window with auto-renew not configured. | Reservation {principal} ({term}) expires on {expiry_date} (in {days_until_expiry} days) and is not configured to auto-renew. Verify whether the workload still needs reserved capacity. If yes, consider renewing or exchanging the reservation before the expiry date. If no, plan for the workload to fall back to on-demand pricing on {expiry_date} and capture the projected on-demand cost in your forecast. |
| `AZ.COMMITMENT_UNDER_COVERED` | medium | ✅ | n/a | Under-utilised reservation while a sibling subscription pays on-demand for a likely-compatible workload. | Reservation {principal} is {scope_kind}-scope and averaged {utilization_pct}% utilisation, while sibling subscription {sibling_sub} shows ${sibling_on_demand_spend_usd} of on-demand resource spend over the same window. Verify the sibling's on-demand SKUs are compatible with this reservation's family and region (Azure auto-applies on a best-fit basis), then consider widening the reservation's applied scope so the sibling's workload absorbs the unused capacity. |
| `AZ.DEV_TEST_SUB_MISMATCH` | medium | ✅ | n/a | Workload tag and subscription offer disagree (prod-in-devtest or vice-versa). | Resource {principal} is tagged env={env_tag} but lives in subscription {subscription_id} ({subscription_offer}). Move the workload to a subscription with the matching offer to avoid mispricing or compliance risk. |
| `AZ.IDLE_VM_14D` | high | ✅ | 14 | VM with <5% CPU and minimal network for 14 days. | Virtual machine {principal} has averaged {avg_cpu_pct}% CPU and {avg_net_kbps} KB/s network over 14 days. Stop or deallocate the VM, or right-size to a smaller SKU. |
| `AZ.LOG_ANALYTICS_OVERINGEST` | medium | ✅ | n/a | Log Analytics workspace ingest above commitment-tier sweet spot. | Workspace {principal} ingested {daily_gb} GB/day on average. Move to commitment tier {recommended_tier} for ~{est_savings_pct}% list-price reduction. |
| `AZ.OVERSIZED_VM` | medium | ✅ | n/a | VM whose P95 CPU and memory both stay below 40% for 14 days. | {principal} ({current_sku}) shows P95 CPU {p95_cpu_pct}% and P95 memory {p95_mem_pct}%. Resize to {recommended_sku} in the same family. |
| `AZ.PUBLIC_IP_UNATTACHED` | low | ✅ | n/a | Standard public IP is not associated with any resource. | Public IP {principal} is unattached and accruing reservation charges. Release it. |
| `AZ.RESERVATION_UNDERUTILIZED` | high | ✅ | 30 | Reservation / Savings Plan utilization below 80% for 30 days. | Reservation {principal} averaged {utilization_pct}% utilization over 30 days. Exchange or shrink the commitment at next renewal. |
| `AZ.SAVINGS_PLAN_ELIGIBLE_SPEND` | medium | ✅ | n/a | Scope has uncovered on-demand spend the Benefit Recommendations API would move under a Savings Plan. | Scope {principal} shows uncovered on-demand spend of ${cost_without_benefit_usd} over {lookback_period}. Azure's Benefit Recommendations API projects ${net_savings_usd} in savings if you verify the workload is steady-state and not the trailing edge of a one-off project, then consider a {term} Savings Plan commitment with an hourly commit of ${recommended_hourly_commit_usd}. |
| `AZ.UNATTACHED_DISK` | medium | ✅ | 7 | Managed disk not attached for 7+ days. | Disk {principal} ({disk_size_gb} GB, {disk_sku}) has been unattached since {last_attached_at}. Delete or snapshot-and-delete. |

## GitHub (`github`)

| Rule ID | Severity | Impl | Inactivity (d) | Summary | Recommendation |
|---------|----------|------|----------------|---------|-----------------|
| `GH.COPILOT_INACTIVE_30D` | high | ✅ | 30 | Copilot Business / Enterprise seat with no acceptances in 30 days. | {principal} holds a Copilot seat but has accepted zero suggestions in 30 days. Reassign or remove the seat. |
| `GH.GHAS_OVER_PROVISIONED` | medium | ✅ | n/a | Advanced Security committer count exceeds active-scanning repos. | GHAS is enabled on {ghas_repo_count} repositories with {active_committers} unique committers in the last 90 days, but only {actively_scanned} repos have produced an alert. Disable GHAS on inactive repos to reduce billable committers. |
| `GH.INACTIVE_SEAT_90D` | high | ✅ | 90 | Enterprise / Business seat with no activity in 90 days. | {principal} has had no commits, reviews, issue activity, or sign-ins in 90 days. Reclaim the {current_sku} seat. |
| `GH.RUNNER_TIER_MISMATCH` | low | ✅ | n/a | Runner-minute spend is materially over or under the current SKU's included quota. | The org consumed {used_minutes} runner-minutes in the last billing period against {included_minutes} included minutes. {tier_action_recommendation} |

## Azure DevOps (`ado`)

| Rule ID | Severity | Impl | Inactivity (d) | Summary | Recommendation |
|---------|----------|------|----------------|---------|-----------------|
| `ADO.INACTIVE_BASIC_90D` | high | ✅ | 90 | Basic seat with no work-item or repo activity in 90 days. | {principal} holds an ADO Basic seat but shows no work-item, code, or pipeline activity in 90 days. Reclaim the seat. |
| `ADO.PARALLEL_JOBS_OVER_PROVISIONED` | medium | ✅ | n/a | Purchased Microsoft-hosted parallel jobs exceed P95 concurrent usage. | The organization owns {purchased_parallel_jobs} Microsoft-hosted parallel jobs but P95 concurrent usage was {p95_concurrent_jobs}. Reduce the purchased count. |
| `ADO.STAKEHOLDER_ELIGIBLE` | medium | ✅ | n/a | Basic seat whose only activity is reading boards / commenting. | {principal}'s only ADO activity in 90 days is reading boards and commenting. Step them down to the free Stakeholder access level. |
| `ADO.TEST_PLANS_UNUSED` | medium | ✅ | 60 | Basic+Test Plans seat with no Test Plans activity in 60 days. | {principal} holds a Basic + Test Plans seat but has not opened or modified a Test Plan in 60 days. Step down to Basic. |

## Advisory triage defaults

The `finops-assess triage` command derives these owner roles and verification checklists from each rule's surface. The artefact is advisory and does not perform remediation.

| Rule ID | Suggested owner role | Verification checklist |
|---------|----------------------|-------------------------|
| `ADO.INACTIVE_BASIC_90D` | ado-org-admin | Confirm the Azure DevOps organisation and project ownership.<br>Review work-item, code, pipeline, and Test Plans activity before changing access.<br>Verify stakeholder eligibility or parallel-job needs with the ADO admin. |
| `ADO.PARALLEL_JOBS_OVER_PROVISIONED` | ado-org-admin | Confirm the Azure DevOps organisation and project ownership.<br>Review work-item, code, pipeline, and Test Plans activity before changing access.<br>Verify stakeholder eligibility or parallel-job needs with the ADO admin. |
| `ADO.STAKEHOLDER_ELIGIBLE` | ado-org-admin | Confirm the Azure DevOps organisation and project ownership.<br>Review work-item, code, pipeline, and Test Plans activity before changing access.<br>Verify stakeholder eligibility or parallel-job needs with the ADO admin. |
| `ADO.TEST_PLANS_UNUSED` | ado-org-admin | Confirm the Azure DevOps organisation and project ownership.<br>Review work-item, code, pipeline, and Test Plans activity before changing access.<br>Verify stakeholder eligibility or parallel-job needs with the ADO admin. |
| `AZ.COMMITMENT_RENEWAL_REVIEW` | azure-owner | Confirm the resource is still owned and in scope for optimisation.<br>Review recent metrics, tags, and change windows before resizing or stopping.<br>Verify reservations, commitments, or environment tags with the FinOps owner. |
| `AZ.COMMITMENT_UNDER_COVERED` | azure-owner | Confirm the resource is still owned and in scope for optimisation.<br>Review recent metrics, tags, and change windows before resizing or stopping.<br>Verify reservations, commitments, or environment tags with the FinOps owner. |
| `AZ.DEV_TEST_SUB_MISMATCH` | azure-owner | Confirm the resource is still owned and in scope for optimisation.<br>Review recent metrics, tags, and change windows before resizing or stopping.<br>Verify reservations, commitments, or environment tags with the FinOps owner. |
| `AZ.IDLE_VM_14D` | azure-owner | Confirm the resource is still owned and in scope for optimisation.<br>Review recent metrics, tags, and change windows before resizing or stopping.<br>Verify reservations, commitments, or environment tags with the FinOps owner. |
| `AZ.LOG_ANALYTICS_OVERINGEST` | finops-analyst | Confirm the resource is still owned and in scope for optimisation.<br>Review recent metrics, tags, and change windows before resizing or stopping.<br>Verify reservations, commitments, or environment tags with the FinOps owner. |
| `AZ.OVERSIZED_VM` | azure-owner | Confirm the resource is still owned and in scope for optimisation.<br>Review recent metrics, tags, and change windows before resizing or stopping.<br>Verify reservations, commitments, or environment tags with the FinOps owner. |
| `AZ.PUBLIC_IP_UNATTACHED` | azure-owner | Confirm the resource is still owned and in scope for optimisation.<br>Review recent metrics, tags, and change windows before resizing or stopping.<br>Verify reservations, commitments, or environment tags with the FinOps owner. |
| `AZ.RESERVATION_UNDERUTILIZED` | finops-analyst | Confirm the resource is still owned and in scope for optimisation.<br>Review recent metrics, tags, and change windows before resizing or stopping.<br>Verify reservations, commitments, or environment tags with the FinOps owner. |
| `AZ.SAVINGS_PLAN_ELIGIBLE_SPEND` | azure-owner | Confirm the resource is still owned and in scope for optimisation.<br>Review recent metrics, tags, and change windows before resizing or stopping.<br>Verify reservations, commitments, or environment tags with the FinOps owner. |
| `AZ.UNATTACHED_DISK` | azure-owner | Confirm the resource is still owned and in scope for optimisation.<br>Review recent metrics, tags, and change windows before resizing or stopping.<br>Verify reservations, commitments, or environment tags with the FinOps owner. |
| `GH.COPILOT_INACTIVE_30D` | github-org-admin | Confirm the seat, org, or repository ownership before changing entitlements.<br>Review recent contribution, Copilot, GHAS, and runner usage signals.<br>Verify security or release-engineering exceptions with the GitHub admin. |
| `GH.GHAS_OVER_PROVISIONED` | github-org-admin | Confirm the seat, org, or repository ownership before changing entitlements.<br>Review recent contribution, Copilot, GHAS, and runner usage signals.<br>Verify security or release-engineering exceptions with the GitHub admin. |
| `GH.INACTIVE_SEAT_90D` | github-org-admin | Confirm the seat, org, or repository ownership before changing entitlements.<br>Review recent contribution, Copilot, GHAS, and runner usage signals.<br>Verify security or release-engineering exceptions with the GitHub admin. |
| `GH.RUNNER_TIER_MISMATCH` | github-org-admin | Confirm the seat, org, or repository ownership before changing entitlements.<br>Review recent contribution, Copilot, GHAS, and runner usage signals.<br>Verify security or release-engineering exceptions with the GitHub admin. |
| `M365.COPILOT_INACTIVE_60D` | license-admin | Confirm the assigned persona and business exception with the license owner.<br>Review recent sign-in and workload activity before changing any license.<br>Verify compliance, mailbox, guest, or break-glass exceptions are not present. |
| `M365.DISABLED_USER_LICENSED` | identity-admin | Confirm the assigned persona and business exception with the license owner.<br>Review recent sign-in and workload activity before changing any license.<br>Verify compliance, mailbox, guest, or break-glass exceptions are not present. |
| `M365.DUPLICATE_BUNDLE` | license-admin | Confirm the assigned persona and business exception with the license owner.<br>Review recent sign-in and workload activity before changing any license.<br>Verify compliance, mailbox, guest, or break-glass exceptions are not present. |
| `M365.E5_FEATURES_UNUSED` | license-admin | Confirm the assigned persona and business exception with the license owner.<br>Review recent sign-in and workload activity before changing any license.<br>Verify compliance, mailbox, guest, or break-glass exceptions are not present. |
| `M365.GUEST_PREMIUM_LICENSED` | identity-admin | Confirm the assigned persona and business exception with the license owner.<br>Review recent sign-in and workload activity before changing any license.<br>Verify compliance, mailbox, guest, or break-glass exceptions are not present. |
| `M365.OVER_LICENSED_VS_PERSONA` | license-admin | Confirm the assigned persona and business exception with the license owner.<br>Review recent sign-in and workload activity before changing any license.<br>Verify compliance, mailbox, guest, or break-glass exceptions are not present. |
| `M365.SHARED_MAILBOX_LICENSED` | license-admin | Confirm the assigned persona and business exception with the license owner.<br>Review recent sign-in and workload activity before changing any license.<br>Verify compliance, mailbox, guest, or break-glass exceptions are not present. |
| `M365.UNUSED_LICENSE_30D` | license-admin | Confirm the assigned persona and business exception with the license owner.<br>Review recent sign-in and workload activity before changing any license.<br>Verify compliance, mailbox, guest, or break-glass exceptions are not present. |
