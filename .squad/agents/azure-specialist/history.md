# Project Context

- **Project:** FinOps-assessment
- **Created:** 2026-05-04

## Core Context

Agent azure-specialist initialized for the FinOps-assessment squad. See `.squad/agents/azure-specialist/charter.md` for role, boundaries, and voice.

## Recent Updates

 Squad team expanded on 2026-05-04 to cover the M0–M7 roadmap (M365, Azure, GitHub/ADO surfaces + security review + test ownership).

## Learnings

- **2026-05-12** , Issues #27 (D.4: Azure pricing intelligence,Noor's recommended pilot), #28 (D.5: CapEx/OpEx commitments), #30 (D.7: agreement-types triage) now in backlog. Pre-commit to OIDC-only auth in issue bodies per stage-4 review so hard rule 2 is not re-litigated in collector PR.
- **2026-05-12** , PR #39 (issue #27): Azure region-price observation contract. File/module placement: observations in `src/finops_assess/pricing.py` (separate from `models.py`) to reinforce observation-vs-catalog boundary. Key pydantic choices: `currency: Literal["USD"]` (explicit single-currency baseline, expandable later), `observed_at: str` constrained to ISO 8601 date (min/max_length=10), `source: Literal[...]` for auditable provenance. Wiring into `NormalizedDataset` intentionally deferred to rule PRs (#28, #30) to avoid speculative design. Open questions for #28/#30: Should commitment pricing (RI/SP) be a subclass or separate model? Should agreement discounts (EA/MCA/CSP) be applied at collection time or rule-evaluation time? Sovereign-cloud support requires security/compliance review first. The observation-vs-catalog separation pattern set here is the foundation for #28 (RI/SP) and #30 (agreement-types) to extend cleanly.
- **2026-05-12** , PR #42 (issue #28): Azure commitment-coverage data model. Extended `pricing.py` observation pattern to RI/SP commitments: separate models for `AzureCommitmentObservation` (utilization, coverage, scope, expiry) and `SavingsPlanEligibleSpendObservation`. Language-guardrail enforcement: docstring-level constraint tested via `test_commitment_language_guardrail()` to block prohibited verbs ("purchase", "buy", "exchange" as imperative actions). Commitment models are **observations** (input data), NOT normalized records , the existing `AzureReservation` in `models.py` is the normalized-record layer consumed by rules; collectors transform observations into normalized records. This layering distinction answers the #27 open question "Should commitment pricing be a subclass or separate model?": it's a separate observation family in `pricing.py`, NOT a subclass of catalog entries. Wiring to `NormalizedDataset` deferred to rule PRs (consistent with #27). #30 (agreement-types) will inherit this pattern: EA/MCA/CSP multipliers as a third observation family. Test count: 13 new tests (32 total in test_pricing.py). All validation gates pass.
- **2026-05-12** , PR #42 round-2 lockout-revision (Diego): Python's `\b` word boundary treats underscore as a word character, so regex patterns like `\bpurchase\b` silently fail against snake_case Literal values like `"purchase_recommended"` or `"exchange_recommended"` , the exact surface the reflective Literal walk was designed to protect. Fixed by replacing `\b` anchors with explicit alphabetic lookaround `(?<![a-zA-Z])(purchase|buy|exchange|modify)(?![a-zA-Z])` (case-insensitive). This catches `exchange_recommended`, `purchase_now`, `BUY_SP` as intended, while still excluding "purchased" (past tense). Added three positive-control assertions and one negative control to prove the fix. This regex gotcha applies to #30 (agreement-types) which will also use Literal enums: use explicit lookaround for token-boundary scanning of snake_case strings.
- **2026-05-12** , PR #30 (issue #30): Agreement-multiplier pricing-profile contract. Third extension of pricing.py observation pattern. Key design choices: (1) Multiplier bounds [0.0, 1.0] , multipliers >1.0 (above list, e.g., sovereign-cloud markup) are out of scope for v1; model them as list-price observations with multiplier=1.0 and document the premium separately. (2) Currency posture: require currency match between profile and observation; rules reject mismatches. No auto-conversion (exchange rates are temporal). USD baseline for v1; future multi-currency expands both contracts together. (3) Scope-vs-product: profiles are scoped (`Literal["azure", "m365", "github", "ado"]`); customers with multiple agreements (EA for Azure, CSP for M365) supply multiple profiles. Rules filter by scope before applying. (4) Effective-date validation: rules are responsible for checking `observation.observed_at` falls within `[effective_from, effective_to]`; the model does not enforce this (observations and profiles are independent inputs). (5) docs/plan.md §10 open-question 1 RESOLVED: pricing profiles are customer-supplied inputs, NOT catalogue constants; default posture is list price (multiplier=1.0, source=default_list). Complete pricing.py surface now has 3 model families (region-price, commitment-coverage, pricing-profile), all sharing pydantic idioms: `extra="forbid"`, ISO 8601 date constraints, `source` provenance fields, `currency: Literal["USD"]` baseline, no hardcoded tenant data. Natural next extension: commitment-discount overlap with agreement-multiplier (rule layer figures out interaction order, not the contract); multi-currency support (expand `currency` in both observations and profiles simultaneously, rules handle conversion); sovereign-cloud regions (add `cloud` scope after security review). Test count: 11 new tests (42 total in test_pricing.py). Language guardrail extended reflectively (PricingProfile auto-discovered). All validation gates pass.
- **2026-05-13** , Issue #58 stage-5 implementation (Diego, PR #70): FOCUS 1.3-aligned advisory CSV exporter shipped for Azure-only v0.5.0. Two learnings promoted to standing memory: (1) **NUL-collision fix in advisory_finding_key():** switched from `rule_id + '\x00' + resource_id + '\x00' + evidence_json` to `sha256(json.dumps([rule_id, resource_id, evidence_json]))`. Vulnerability: evidence values containing literal NUL bytes could collide with distinct tuples. Fix: JSON serialization is lossless and guarantees unique canonical form. Consequence: bumped Rule.evidence_key_version to explicit 1, creating forward contract for v0.6.0 hash changes. (2) **_determinism.py shared module pattern:** lifted `_generated_at()` from json_reporter to a dedicated `src/finops_assess/reporters/_determinism.py` module. Reusable for any reporter needing SOURCE_DATE_EPOCH determinism. Files: focus_aligned.py (378 lines), _determinism.py (44 lines), manifest schema + bundled loader, 11 test cases with golden artifacts, full docs + examples. All validation gates pass (finops-assess validate, ruff, mypy, pytest 211/211). Noor's stage-4 verdict approved. Yuki hardening review in progress; #71 tracks v0.6.0 multi-surface D7 expansion (M365, GitHub, ADO, GCP/AWS).
- **2026-05-13** , Issue #61 (playbook / ticket reporter, v0.5.0) stage-3 plan locked; Diego assigned as implementer + Yuki as tests/docs/golden-fixtures collaborator. Plan committed to `.squad/decisions.md:6-500`. Four divergences locked: D1 both-additively (row v1 shape + optional adapter_hints), D2 Option B-honest (ticket_key + finding_revision + per-surface stability declaration), D3 both-complementarily (pre-compile + StrictUndefined), D4 evidence_ref + template_render_inputs. Research OQs (OQ1–5) closed. Noor predictions (N1–N5) pre-empted. Two follow-ups filed: #73 (stable-salt engine, v0.6.0 p1), #74 (runtime overlay sandbox, v0.6.0 p2). Architectural pattern reuse from #58: exporter/manifest/golden-fixture shape, byte-contract discipline, determinism helper, single-module-per-format convention. Noor stage-4 review spawned; verdict pending. PR #72 open (draft, squad:maya + release:v0.5.0).



- **2026-05-13 11:58Z** , Issue #61 / PR #78 revision under Reviewer Rejection Lockout. **Diego Learning: Locked out of lockout-revision — the protocol works as designed.** When stage-4 verdict is plan-rooted (BLOCKING #1 stemmed from an asserted false assumption in Maya's stage-3, not from a Diego implementation drift), the implementer is locked out. Yuki picked up the revision and fixed it (commit 5bf48e8, 15 files, +1742 / -847). Key insight: **the lockout is not a punishment**, it's a lock that prevents confirmation bias. Diego implemented the locked plan faithfully; locking Diego out prevents him from defending the plan's assumptions. Fresh eyes (Yuki's hardening expertise) did the work. This is the pattern working correctly. **Atomic-write Option C is now a binding pattern:** once this PR merges, all future multi-file exports use the manifest-as-readiness-marker + os.fsync + sha256 self-attestation + orphan-recovery pattern. No variation. Rule of thumb: when a stage-4 verdict cites "the plan assumed X without verifying Y", lock out the plan author, not the implementer. When it cites "the implementer drifted from locked design", lock out the implementer. The stage-3 plan document should be explicit about which assumptions are being verified at what stage (stage-4 empirical review vs stage-5 code review). Full verdict at .squad/decisions.md §11 Stage-4 Adversarial Review and follow-up issues #81/#82 filed (Yuki's assessment, routed accordingly).
- **2026-05-13 12:15Z** , PR #78 merged to main as commit `f4eae9d` (squash-merge by Coordinator). Playbook / ticket reporter (issue #61) shipped for v0.5.0. **Diego Learning: Option C atomic-write is the binding pattern.** The implementation under #61's locked plan used mkstemp() + fsync() + os.replace() for JSONL/manifest pair, manifest written after JSONL as the readiness signal, --cleanup-orphans flag with pre-flight scan for stranded files. Noor's original stage-4 review flagged "Atomic-write Option C: correctly implemented" as one of the clean findings; the revision by Yuki preserved this pattern without modification. Final state (commit 5bf48e8 → f4eae9d squash): the atomic-write implementation is clean and becomes the blueprint for future multi-file exports (rules: JSONL first, manifest second, fsync before replace, manifest presence is readiness signal, orphan-recovery always paired). No variation is permitted. The pattern is now binding.

- **2026-05-13 17:45Z** , Issue #59 / rule 1 of 5 stage-5 implementation complete (PR #85, Diego). **Key implementation decisions:** (1) **Collector deduplication strategy:** ARM collector implements the lookback-priority canonical observation rule from Maya's plan §3.4 (Last60Days > Last30Days > Last7Days), then de-duplicates per `(scope, term)` in-memory before CSV write. Rule body then adds a second dedup pass to guard against CSV fixtures bypassing the collector. (2) **Rule model extension:** Added `min_uncovered_usd: float | None` field to Rule model (line 55) to support YAML-tunable threshold (NIT #3). Default is 50.0 in YAML. (3) **Playbook template:** Used full TITLE/DESCRIPTION/REMEDIATION_STEPS/VERIFICATION_CHECKLIST/REFERENCES structure mirroring `AZ.RESERVATION_UNDERUTILIZED.j2`, not a prose-only template. Template variables come from spread evidence dict (context build at `playbook.py:387`). (4) **Friendly enum error handling:** While pydantic Literals will hard-fail on unknown term/lookback values today (NIT #4), the model design is extensible — a future PR could replace Literals with constrained str + validation that logs unknown values. This is deferred as out-of-scope for rule 1 (no known API additions yet). (5) **Test pattern:** E2E regression test (`test_savings_plan_e2e_through_run_rules`) uses real `run_rules` engine with synthetic `NormalizedDataset`, not a mocked rule callable. This is the Yuki-net pattern from PR #78 that caught BLOCKING #1. All 10 unit tests pass + 497 tests in full suite (1 PDF failure unrelated, pre-existing WeasyPrint dep issue on Windows). All validation gates green: finops-assess validate, ruff check, ruff format, mypy src, pytest. Doc regen committed in same PR. **Gotchas for Yuki/future implementers:** (a) RuleContext signature changed between PR #42 (pricing.py observations) and now — use `catalog`, `catalog_list`, `personas`, `persona_assignments`, `assignments_by_principal`, `usage_by_principal` (not `catalog_by_id`, `personas_by_id`, `assignments_idx`, `usage_idx`). (b) Playbook template LF line endings are enforced by `tests/test_playbook_template_lf.py`. On Windows, use PowerShell `[System.IO.File]::WriteAllText` with UTF8 no-BOM encoding to force LF. (c) ARM collector `sub_ids` is the variable name for subscription list (not `subscriptions` dict). (d) Maya's plan specifies `benefit_kind: Literal["SavingsPlan", "Reservation"]` but the collector hard-codes `"SavingsPlan"` as default in the CSV write — this matches NIT #2 (filter to SavingsPlan only) and avoids introducing a Reservation-eligible-spend rule prematurely. (e) `generate_docs.py` must be run after template creation and changes committed together, or CI docs-check gate fails.
- **2026-05-13  ,  Diego assigned: Rule-2 stage-5 implementation (AZ.COMMITMENT_UNDER_COVERED  ,  #59 rule 2/5).** Maya stage-3 plan submitted to Noor for stage-4 verdict. **Primary role:** pending Noor approval  ,  Diego is the named stage-5 implementer. **Plan summary:** zero schema changes  ,  derived-view rule over existing AzureReservation (models.py:235-250) and AzureResource (models.py:206-232) with group-by subscription_id on under-utilised reservations. Design invariants: group-by subscription_id (sibling-sub join key)  ,  utilization_pct < 80% gate (dual-fire overlap with AZ.RESERVATION_UNDERUTILIZED intentional  ,  rule #9 in §3.8 tests this), two redaction surfaces (principal = reservation_id  ,  evidence["sibling_sub"] = subscription_id  ,  both ctx.redact()). Three R-alternatives walked and rejected (R1: extend AzureReservation with applied_scope — belongs to rule 4  ,  R2: add AzureSubscriptionCost — YAGNI without Cost Mgmt collector  ,  R3: reuse rule-1's AzureBenefitRecommendation — conflates two operationally distinct findings). **Stage-3 correction:** epic #59 framing says "Cost Mgmt + reservation list" but arm_collector.py:35-44 does NOT call Cost Mgmt; rule operates on AzureResource.monthly_cost_usd (unpopulated in live ARM  ,  populated in CSV-mode). Noor will verify this independently. **Test expectations:** plan §3.8 lists 9 tests; binding surfaces: test #1-4 (basic eligibility  ,  scope join  ,  utilization gate  ,  cross-rule overlap proof); test #5-6 (benefit recommendation text match  ,  evidence["sibling_sub"] attestation); test #7-8 (redaction symmetry  ,  principal+recommendation+evidence binding); test #9 (overlap regression). **Code patterns available from prior stages:** language guardrail (prohibited verbs  ,  uses lookaround not \b boundaries per #42 fix)  ,  cross-run regression test pattern (#78 playbook  ,  for any manifest-claim invariants)  ,  atomic-write Option C (binding for multi-file exports; not applicable to this rule  ,  single-rule JSON output). **Implementation boundary:** the rule must match plan §3.5 rule body exactly; no drift. Binding scope: src/finops_assess/rules_impl/azure_rules.py (new rule class  ,  ~60 lines based on similar rules), tests/test_azure_rules.py (9 new test cases  ,  ~300 lines), data/rules/azure.yaml (new YAML entry with id=AZ.COMMITMENT_UNDER_COVERED  ,  matching plan §3.6). Noor verdict pending. If Noor REJECTS: lockout fires  ,  Diego cannot revise  ,  routes to different agent.
- **2026-05-13** , Diego (azure-specialist) stage-5 implementer assignment: You are named as stage-5 implementer for AZ.SAVINGS_PLAN_ELIGIBLE_SPEND (#59 rule 1/5) once Noor completes stage-4 adversarial review on PR #83. New implementation will open on separate branch squad/59-impl-savings-plan-eligible as a new PR (not merged into #83). Core deliverable: new pydantic model AzureBenefitRecommendation in src/finops_assess/models.py, no catalogue additions, new rule in src/finops_assess/rules/azure.yaml, new rule implementation. Yuki is your backup. Noor's 10 invariants are the acceptance criteria.
- **2026-05-13  ,  Diego assignment: Stage-5 implementation for AZ.SAVINGS_PLAN_ELIGIBLE_SPEND (#59 rule 1/5, PR #83 now approved by Noor).** Branch: squad/59-impl-savings-plan-eligible (separate PR, opens after plan approval). Five non-blocking NITs from Noor's stage-4 verdict for your consideration during implementation: (1) Recommendation wording — "consider the commitment purchase" contains noun "purchase"; consider rewording to "consider the commitment" or "consider the Savings Plan commitment" to fully satisfy the substring filter in invariant 6 (substantively conservative already, this is a polish). (2) Filter on benefit_kind — the AzureBenefitRecommendation.benefit_kind field is Literal["SavingsPlan", "Reservation"] but the rule body does not filter; add if rec.benefit_kind != "SavingsPlan": continue so Reservation recommendations don't surface with misleading SP-specific wording. (3) Threshold tunability — expose min_uncovered_usd: 50.0 as a YAML rule field (add min_uncovered_usd attribute to Rule model if not present) so operators can tune the noise floor without code change, matching peer-rule pattern from AZ.IDLE_VM_14D. (4) Literal strictness against API additions — Literal["P1Y", "P3Y"] and Literal["Last7Days", "Last30Days", "Last60Days"] will hard-fail if Microsoft adds new term/lookback values; surface a clear friendly error message ("unrecognised Benefit Recommendation term / lookBackPeriod — file an issue") instead of raw pydantic stack trace. (5) REQUIRED_RULES set — tests/test_engine.py:23 REQUIRED_RULES currently lists 7 Azure rules; add the new rule ID as the 8th (acceptance criterion #10 in Noor's invariant list). None of these NITs gate the implementation PR; all 10 invariants passed. Yuki remains backup.
