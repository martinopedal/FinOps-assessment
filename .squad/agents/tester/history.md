# Project Context

- **Project:** FinOps-assessment
- **Created:** 2026-05-04

## Core Context

Agent tester initialized for the FinOps-assessment squad. See `.squad/agents/tester/charter.md` for role, boundaries, and voice.

## Recent Updates

 Squad team expanded on 2026-05-04 to cover the M0–M7 roadmap (M365, Azure, GitHub/ADO surfaces + security review + test ownership).

## Learnings

- **2026-05-12** , Issues #24 (D.1: SKILL rewrite,project-conventions docs-to-code sync), #32 (D.9: first runbook,automated triage & reporting) now in backlog. Review test coverage for incoming D.4–D.8 collector PRs as they land; these spikes are gated on stage-4 falsification criteria per the pilot frame.
- **2026-05-12** , Issue #24 completed (PR #34): `.squad/skills/project-conventions/SKILL.md` rewritten from placeholder template by paraphrasing copilot-instructions.md (hard rules 1–5, tech stack, validation gates) and docs/plan.md §1 (guiding principles, catalogue/package model). Agents now read authoritative convention guidance at session start instead of template noise. All validation gates passed (finops-assess validate ✓, ruff ✓, mypy ✓, pytest 121/125 ✓).
- **2026-05-13** , Issue #32 (D.9: first runbook,"Run assessment") completed. Runbook format chosen: Markdown with H1 title, Purpose one-liner, Prerequisites checklist, numbered step-by-step (keyed to actual shipped CLI commands only), (planned) section for unshipped features explicitly marked with the literal `(planned)` suffix (not italics), read-only posture statement (5 check/cross bullets), and See Also links. Updated `docs/skills/README.md` index to link and mark row as 🟢 (runbook ready). Convention for multi-skill PRs (one runbook per PR per issue) now established. Validation gates baseline: 87/7/23 maintained. Hard rules preserved: read-only in runbook steps (no write/admin scopes), no secrets/PII (only synthetic examples), no vendor doc copying (paraphrase + link), PII redaction guidance included. Planned to reuse format for converting remaining 5 exploratory rows to runbooks later.
- **2026-05-13** , Lockout-revision on PR #42 (issue #28: Diego's commitment-coverage data model). Noor's review surfaced two findings: (1) scope violation , six §11 stage scratch files (`pr-body.md`, `stage-1..5-*.md`) committed to repo root, removed in commit `9c6869e`; (2) test coverage gap in `test_commitment_language_guardrail()` , only checked two class docstrings, missed `AzureCommitmentDataset`, all Field descriptions, and all Literal enum values. Expanded test to iterate every commitment model, extract all surfaces (docstrings via `__doc__`, Field descriptions via `model_fields[name].description`, Literal values via `typing.get_origin`/`get_args` recursive unwrap), and check prohibited-verb regex (`\bpurchase\b`, `\bbuy\b`, `\bexchange\b`, `\bmodify\b`) against concatenated corpus. Failure messages now name model + surface for fast diagnosis. Pattern reusable for future pricing.py contracts (issue #30 will likely need this same guardrail). Expanded test caught NO existing offenders , Diego's models already adhered to read-only posture. All validation gates passed (ruff ✓, mypy ✓, pytest 167/167 ✓, finops-assess validate ✓). Posted revision summary to PR #42; awaiting Noor's re-review per protocol.
- **2026-05-13** , Issue #44 completed (PR #46): Fixed `tests/test_live_collectors.py` failing on fresh `[dev]` install. **Root cause:** `[dev]` extras install `types-requests` (mypy stubs) but not `requests` runtime; `[live]` extras install `requests>=2.32`. The 6 live-collector tests lazily import collector modules that depend on `requests`, causing `ModuleNotFoundError` at test runtime when `[live]` not installed. CI was green because `.github/workflows/ci.yml:34` installs `[dev,live]`. **Solution (Option A):** Added `pytest.importorskip("requests", reason="Live collectors require [live] extras: pip install -e .[live]")` at module level in `tests/test_live_collectors.py` before any imports. Pattern: `pytest.importorskip(module_name)` is pytest's canonical guard for optional-dependency tests , checks importability at module load time, skips entire module with clear message if unavailable. **Verification harness:** (1) Parent venv with `[live]`: `pytest tests/test_live_collectors.py -v` → 6 PASSED; (2) Fresh `[dev]`-only venv: `python -m venv .venv-dev-only && pip install -e .[dev]` → same pytest invocation → 1 module SKIPPED; (3) Full suite: 189 passed, 3 skipped (no regressions). **Learnings:** The `[dev]`/`[live]` extras boundary is intentional , dev tooling (pytest, mypy, ruff, type stubs) vs production deployment dependencies (azure-identity, requests). Type stubs allow type-checking collector code without installing runtime deps. When tests exercise optional-dependency code paths, guard with `importorskip` at module level rather than conditional imports or merging extras. Fresh-venv verification is critical for extras-boundary PRs , parent venv accumulates dependencies across sessions and hides missing-dep failures. **Scratch-file hygiene:** Added `pr-body.md` and `stage-*.md` patterns to `.gitignore` per Noor's #42 lockout lesson , §11 ceremony artefacts belong in PR body, not repo root. **CI expectation:** No change; CI already installs `[dev,live]` so `importorskip` is a no-op in CI matrix.
- **2026-05-13** , Issue #53 docs-voice sweep (PR #55, Maya + Yuki): Mechanical application of `.squad/skills/docs-voice/SKILL.md` across 42 in-scope files. **Sweep results:** 714 em-dashes replaced with commas/periods (highest volume), AI-language blacklist scrubbed (3 hits: "additionally-assigned" → simplified, "robust pattern" → removed, live in 39 files), decorative emojis stripped (87 total, kept functional badges ✅❌🏗️⚛️🔧🧪📋🔄), SKILL pointer inserted in `.github/copilot-instructions.md`. **Execution strategy:** (1) Baseline with `git diff --stat` to verify no CRLF blowup (proportional line deltas, no 100x explosion); (2) em-dash sweep first (script-driven, bulk change); (3) AI-language fixes targeted (context-sensitive edits for "robust" + "additionally"); (4) emoji sweep (low-volume, edit tool); (5) SKILL pointer insertion (one surgical line); (6) whitespace verification (`git diff --check`). **Learnings:** When porting a voice from different surfaces (LinkedIn → docs), the SKILL is the authoritative single location, no parallel `docs/voice/` page to avoid drift. Commas are the default em-dash replacement (list-aside context), periods and "and" only when two independent clauses. The `[System.IO.File]::ReadAllText/WriteAllText` pattern in PowerShell is critical for CRLF preservation on Windows , `Get-Content`/`Set-Content` mangle line endings. Verification (line count proportionality, zero trailing whitespace) prevents silent regressions. All validation gates passed (ruff, mypy, finops-assess validate).
- **2026-05-13** , Issue #58 stage-5 completion (Diego, PR #70, Noor approved, Yuki hardening in progress). FOCUS 1.3-aligned advisory CSV exporter for Azure-only v0.5.0. **Test delivery:** 11 new test cases in test_focus_aligned_reporter.py (534 lines) covering CSV shape, manifest fields, determinism (SOURCE_DATE_EPOCH), golden artifacts, advis finding-key stability, surface filtering, empty inputs, cost-field validation. 7 golden fixture files (CSV, manifest, CLI help, 3 input JSONs) pinning expected outputs. Pattern: focus_aligned reporter pairs with _determinism.py shared module for deterministic timestamp handling. All 211 tests passing (finops-assess validate 87/7/23, ruff, mypy strict, pytest with weasyprint skips pre-existing). Noor stage-4 findings: 6 blockers all addressed, all hard rules PASS, P2 items folded inline by Diego. Follow-up #71 tracks v0.6.0 multi-surface expansion.
- **2026-05-13** , PR #70 hardening review (Yuki). **Defect found and fixed:** `tests/fixtures/focus_aligned/golden-azure.csv` and `golden-azure.manifest.json` were missing `text eol=lf` in `.gitattributes`. Windows-hosted CI runners with `core.autocrlf=true` checked them out with CRLF, causing `test_golden_csv_byte_identical` and `test_golden_manifest_byte_identical` to fail on `windows-latest/py3.11` and `windows-latest/py3.12` (confirmed in CI run 25785403902). **Fix:** two `text eol=lf` lines appended to `.gitattributes`. **Hardening tests added (3 new):** NUL bytes inside evidence values (regression test for the sha256 json-envelope fix), Unicode evidence round-trip (emoji/RTL/supplementary planes), long resource_id >1024 chars no truncation. Total test count for the FOCUS reporter: 25. **Cross-platform lesson:** every golden fixture file that is byte-compared in tests needs its own `text eol=lf` entry in `.gitattributes`. The `examples/` artefacts were correctly pinned but the `tests/fixtures/` golden files were not. Add this check to any reporter PR that ships golden files. See `.squad/skills/focus-aligned-golden-fixtures/SKILL.md` for the reusable pattern.
- **2026-05-13 08:55:30Z** , Issue #61 stage-3 plan revision (Yuki, Opus 4.7 xhigh exception). **Context:** Maya's #61 stage-3 plan REJECTED by Noor (2B/8A/3N). Reviewer Rejection Lockout fired; Maya locked out of revising. Yuki (QA/hardening specialist) promoted to plan reviser under Opus 4.7 xhigh exception (stage-3 work mandates Opus regardless of agent role baseline). **Task:** Address all 13 findings without tampering with D1/D2/D3/D4, OQ-1..5, §9 deferred-disposition (the locked architectural decisions). **Outcome:** 100% closure. B1 (manifest pair-atomicity) fixed via **Option C atomic-write pattern** (manifest-as-readiness-marker + os.fsync + sha256 self-attestation + orphan pre-flight + --cleanup-orphans recovery). B2 (.j2 LF-pin regression) fixed at source via `.gitattributes` entry `src/finops_assess/data/playbooks/**/*.j2 text eol=lf` + test #16 regression net. A3–A10 amendments (CLI warning conditions, docs outline, adapter tables, pre-compile scope, access-tracking mechanism, schema fields, tiebreaker, duplicate removal) all addressed. N11–N13 NITs (test promotion to required, known_limitation assertion, schema-versioning docs) all addressed. **No scope added. No new architectural decisions.** Full revision appended to `.squad/decisions.md` under `### Stage-3 Plan Revision (Yuki, post-Noor stage-4 reject)` subsection (commit 10096cb). **One-time role expansion:** Yuki's default is QA/hardening on Sonnet 4.6; this stage-3 planning work is a one-time Opus 4.7 xhigh exception. The lockout precedent is now established. **Atomic-write Option C precedent:** two-step os.replace with fsync durability + manifest-as-readiness-marker + sha256 self-attestation + orphan recovery is now binding for all multi-file exports (reporters, future collectors). Pattern reusable for any future JSONL + manifest sidecar export. **Learning:** Hardening-precedent walk-through (checking if past patterns apply to new scope) is a critical stage-3 planning artifact. The #58 LF-pin fix was a regression for #61 only because Maya's planning checklist did not explicitly ask "which patterns from recent PRs apply here?" Add this to stage-3 checklist guidance for future plans. **Noor re-review:** spawned; verdict pending before Diego stage-5 implementation can begin.
- **2026-05-14** , PR #78 stage-5 revision under reviewer-rejection lockout (Yuki, commit `5bf48e8`). **Trigger:** Noor's stage-4 verdict on PR #78 was REJECT (1 BLOCKING + 3 amendments + 2 NITs); Maya/Diego locked out per protocol; Yuki promoted to revision author. **BLOCKING #1 root cause:** the manifest declared `pii_handling.ticket_key_stability_by_surface = {"azure": "stable", others: "per_run"}` regardless of `pii_redaction` state. Empirical verification with a 5-line reproducer showed `engine.py:RuleContext.redact()` calls `secrets.token_hex(16)` per run and salts EVERY principal when `redact_pii=True` (default), including Azure resource IDs. Both Maya's #72 plan and Yuki's own revised plan (#61 stage-3 reviser session) asserted "Azure resource_id is not PII therefore stable" without inspecting the producer code path. The engine's contract is "salt every redacted value", full stop. **Fix path A:** make `pii_handling.ticket_key_stability_by_surface` a function of `pii_redaction` — `dict.fromkeys(SURFACES, "per_run")` when True, `dict.fromkeys(SURFACES, "stable")` when False. Mirror in `focus_aligned.py` (`pii_handling.mode` becomes one of `azure_resource_id_per_run_salted_hash` / `azure_resource_id_cleartext` + `join_keys[*].stability` conditional). **Regression net:** new `tests/test_playbook_cross_run_stability.py` runs the real engine end-to-end twice with `redact_pii=True` and asserts ticket_keys ROTATE; second variant with `redact_pii=False` asserts they're STABLE. This is the test that would have caught the bug before merge — and is now the binding pattern: any rule that produces a manifest claim about ticket_key/finding_key stability MUST be backed by a real-engine cross-run regression test, not a synthetic-input unit test. **Plan A12 correction:** Diego shipped `note` instead of plan-spec `known_limitation`; Yuki reverted to plan (schema now requires `known_limitation` string|null). This was a Diego deviation correction, not a new deviation. **Cross-cutting learnings:** (1) **False-assumption pattern:** plans MUST cite the producer code path (file:line) that establishes any claim about a manifest field's value. "X is stable because Y is not PII" is unverified speculation unless `engine.py` or equivalent producer is named. Add to stage-3 plan checklist: "every pii_handling/manifest field claim cites producer code path". (2) **`functools.cache` not `lru_cache(maxsize=None)`:** ruff UP033 ratchet requires the shorter form on Python 3.11+. (3) **Click 8.3+ CliRunner:** `mix_stderr=False` removed; `result.stderr` is auto-separated. Don't pass `mix_stderr` kwarg. (4) **Render-context boundary:** when spreading user-controlled dict into a render context with reserved keys, spread USER FIRST then RESERVED SECOND so reserved wins. The reverse order silently lets evidence override `principal`/`recommendation`/etc. (5) **Schema rename in additive-only spec:** technically a breaking change, but `additionalProperties: true` + the original plan having specified `known_limitation` (Diego's `note` was the deviation) means the rename is "correcting to plan", not "introducing a break". Document the rationale in the schema delta and the decision drop. **Test count delta:** 444 → 476 passed (+32 across 4 new test files + parametrized additions). All gates green: `finops-assess validate` (87/7/23), `ruff check`, `mypy --strict`, `pytest` (pdf modules excluded, local weasyprint env gap; CI matrix is the cross-platform truth). **Follow-up issues filed:** #81 (repo-wide `*.py text eol=lf` hygiene — Yuki B7 scope (b)), #82 (B8-10 NIT bundle — `reset_playbook_env` fsync docstring, `get_playbook_env` naming, loop-var shadowing). **Lockout precedent reinforced:** when stage-4 reject fires, the next reviser owns ALL findings (Noor's + own prior amendments) in a single revision pass. No forking, no new PR. The prior reject stays in PR history as the stage-4 record.

- **2026-05-14** , PR #78 cross-platform hardening review (Yuki, commit `eef9b10`). **Scope:** Issue #61 v0.5.0 playbook/ticket reporter; Diego's stage-5 implementation on `squad/61-impl-diego`. **Verdict: APPROVE WITH AMENDMENTS** (0 blocking, 7 amendments, 3 NITs). **Validation gates:** all 5 pass (validate 87/7/23, ruff, mypy strict, pytest 444/3-skipped, docs-check). **CI matrix:** 11/11 green. **Yuki regression class:** PASS — all 23 .j2 templates LF-clean, no BOM, no NUL, UTF-8 clean; `.gitattributes` LF-pin at line 40 confirmed; golden fixtures LF-clean; JSONL write uses `newline=""`. **Atomic-write Option C:** PASS — tempfile in same dir, fsync before os.replace, SHA-256 from on-disk file after rename, manifest written after JSONL rename completes. **9 test files present.** **Key deviations from binding plan:** (A-1) `Rule.adapter_class` missing from `models.py`; `_build_adapter_class_map()` swallows `AttributeError` silently via `except Exception: return {}` — all rules fall back to `"generic"`; (A-2) `pii_handling.known_limitation` absent, `note` shipped instead (A12 violation); (A-3) module-level env singleton violates A7; test #17 absent; (A-4) static AST walk instead of `_AccessTrackingEvidence` (A8 deviation locked into schema description); (A-5) NUL-byte regression test missing from `test_playbook_template_lf.py`; (A-6) `--cleanup-orphans` CLI flag not tested via CliRunner; (A-7) all new Python source files committed with CRLF (pre-existing repo-wide issue perpetuated — repository has no `*.py text eol=lf` in `.gitattributes`). **Cross-cutting learnings:** (1) `except Exception: return {}` in production code silently masks attribute errors on Pydantic models — always narrow the except clause or log before swallowing; (2) CRLF is now in 15+ new files; a follow-up hygiene issue should add `*.py text eol=lf` and `*.yaml text eol=lf` to `.gitattributes` and run `git add --renormalize`; (3) when reviewing reporter PRs, always check for NUL-byte assertion in template regression tests AND for `known_limitation` vs `note` semantic distinction in pii_handling; (4) `subprocess.run(capture_output=True)` is required for raw-byte git object inspection on Windows — PowerShell pipes add `\r`, making byte-level line-ending checks unreliable via shell pipes; (5) sort-key deviations from plan spec (`(surface, rule_id, ticket_key, evidence_ref)` vs `(surface, rule_id, principal, finding_revision, original_index)`) should be flagged even when internally consistent, because schema contracts affect downstream consumers. **PR comment posted:** [issuecomment-4439756631](https://github.com/martinopedal/FinOps-assessment/pull/78#issuecomment-4439756631).


- **2026-05-13** , Yuki (tester) backup implementer assignment: You are backup implementer for AZ.SAVINGS_PLAN_ELIGIBLE_SPEND (#59 rule 1/5). Diego is primary. Stage-3 plan at PR #83. Stage-5 tests will mirror your Yuki-net e2e regression pattern from PR #78 (real run_rules engine, not mocked rule callable). Focus on cross-run stability regression tests for the benefit recommendations and the ctx.redact() principal hashing circuit.
- **2026-05-13** , PR #85 stage-5 lockout revision (Yuki, Diego locked out per Reviewer Rejection Lockout). 3 BLOCKING from Noor: M1 (collector wrote `properties.scope` discriminator into ARN-shaped `scope` field, collapsing multi-sub tenants to <=4 dedup rows), M2 (`docs/plan.md` §6 missing the new rule entry), M3 (hardcoded `benefit_kind="SavingsPlan"` made the NIT #2 rule-side filter dead code in production). Fixed via new pure helper `_normalise_benefit_recommendation` in arm_collector.py: parses `properties.subscriptionId` (+ optional resourceGroup) for Single-scope ARNs, parses parent path from `id` URL for Shared-scope, parses top-level `kind` discriminator for benefit_kind, and applies friendly enum guards (N4 fix at the same surface). Added 8 new regression tests in test_az_savings_plan_eligible.py + 2 strict-column round-trip tests in test_csv_collector.py. Plan amendment committed at docs/plans/059-az-savings-plan-eligible-spend.md §1.1 to record the discriminator-vs-ARN learning per Noor's note-for-Maya. All gates green locally (validate, ruff, ruff format, mypy strict, pytest 507 passed, docs --check). Pattern reinforced: when a stage-3 brief enumerates allowed values of a polymorphic API field, disambiguate the discriminator from the ARN by name in the prose, not just the values. The lockout protocol worked as designed: fresh eyes (Yuki) caught the discriminator-vs-ARN swap that the original implementer (Diego) had locked into a self-consistent but wrong pattern.
- **2026-05-13  ,  Yuki backup assignment: Rule-2 stage-5 implementation (AZ.COMMITMENT_UNDER_COVERED  ,  #59 rule 2/5).** Maya stage-3 plan submitted to Noor for stage-4 verdict. **Backup role:** if Noor approves and Diego (primary) hits blockers or becomes unavailable  ,  Yuki slides in as stage-5 implementer. **Relevant patterns from prior stages:** (1) stage-5 for a derived-view rule (no schema changes) is lower-churn than schema-extension PRs like #28 and #42  ,  tests focus on derived-view correctness + redaction surfaces rather than new Field validation; (2) cross-rule overlap regression test analogous to tests/test_playbook_cross_run_stability.py  ,  both AZ.COMMITMENT_UNDER_COVERED and AZ.RESERVATION_UNDERUTILIZED gate on utilization_pct < 80% so test #9 in plan §3.8 asserts this overlap and that the rule fires independently  ,  not as a logic bug; (3) redaction semantics from #78: two surfaces per finding (principal = reservation_id  ,  evidence["sibling_sub"] = subscription_id  ,  both through ctx.redact())  ,  test #7/#8 enforce symmetry across principal  ,  recommendation  ,  evidence dict as learned in the playbook reporter cross-run hardening; (4) V1 limitations wording (E11/E5/E6) follows conservative-recommendation pattern from Noor's docs-voice decision; (5) cost-signal degradation in live ARM mode (signal unpopulated until future Cost Mgmt collector) has precedent in the #61 playbook reporter gap handling. Binding design invariants: zero schema changes, derived view over AzureReservation + AzureResource, group-by subscription_id on underutilized reservations, matches plan §3.5 rule body exactly, all four redaction call sites per §3.5 match test #7/#8 contract. Noor verdict pending.
- **2026-05-13** , Yuki (tester) backup implementer assignment: You are backup implementer for AZ.SAVINGS_PLAN_ELIGIBLE_SPEND (#59 rule 1/5). Diego is primary. Stage-3 plan at PR #83. Stage-5 tests will mirror your Yuki-net e2e regression pattern from PR #78 (real run_rules engine, not mocked rule callable). Focus on cross-run stability regression tests for the benefit recommendations and the ctx.redact() principal hashing circuit.
- **2026-05-13  ,  Yuki Reviewer Rejection Lockout revision shipped (PR #85, stage-5 implementation APPROVED after revision).** Verdict: **APPROVED** after Diego lockout → Yuki revision → Noor re-review. Revision: commit e289de4, 15 files, +1742/-847. **Fixed all 3 BLOCKING items:** (M1) AzureBenefitRecommendation.scope now correctly reads from the ARN-derived `properties.subscriptionId` field (not the discriminator), disambiguated by plan amendment at §1.1 (Yuki surfaced the brief ambiguity and wrote the correction prose); (M2) docs/plan.md §6 updated with explicit feature descriptions and examples; (M3) hardcoded `benefit_kind="SavingsPlan"` replaced with filtering rule logic (`if recommendation.benefit_kind != "SavingsPlan": continue`), test case added for Reservation filtering. Also incorporated all 5 non-blocking NITs: recommendation wording polished, `min_uncovered_usd` exposed as YAML tunable, friendly error message for unknown API discriminators (replaces pydantic stack trace), REQUIRED_RULES updated in test_engine.py. **Gotchas fixed (Yuki learning notes):** (a) Discriminator-vs-ARN pattern: always demand prose-level clarity in stage-3 briefs when APIs surface polymorphic fields; enumerate the discriminator but also name the ARN source field explicitly. (b) Cross-file contracts (e.g., plan document updates tied to code changes) need to be checked in stage-4 review before approval; a future stage-3 plan template should stamp this as mandatory. (c) Hardcoded defaults that appear "temporary" (benefit_kind="SavingsPlan") are usually a sign that stage-3 assumptions weren't fully verified against the API; spot-check briefs that rely on unstated field defaults. **Test results:** All 497 tests pass (including Diego's 10 unit tests + Yuki's 6 new regression tests); CI all-green; finops-assess validate, ruff, mypy, pytest clean. **Pattern reuse:** The Reviewer Rejection Lockout protocol from PR-#78 is now a binding precedent (see .squad/decisions.md §11 Stage-4 Adversarial Review). Noor's re-review verdict: https://github.com/martinopedal/FinOps-assessment/pull/85#issuecomment-4441639618 (APPROVED, no further changes requested).

- **2026-05-14** , PR #88 stage-5 lockout revision (Yuki, Diego locked out per Reviewer Rejection Lockout, second lockout cycle this session after PR #85). Noor verdict: 5 MUST-FIX + 2 NIT, REQUEST_CHANGES. **All 7 items resolved in one commit:** (MF1) playbook `AZ.COMMITMENT_UNDER_COVERED.j2` template made StrictUndefined-safe -- four undefended evidence references (`scope_kind`, `sibling_sub` x2, `sibling_on_demand_spend_usd`) wrapped with `{{ X if X is defined else '...' }}` guards mirroring the line-6 `utilization_pct` precedent and the sister `AZ.SAVINGS_PLAN_ELIGIBLE_SPEND.j2` pattern from PR #85; root cause was the reporter spreading `evidence={}` first into the StrictUndefined Jinja namespace at `reporters/playbook.py:386-388`. (MF2) `tests/test_csv_collector.py:19` count assertion bumped from `== 5` to `== 6` to match the appended sibling row in `samples/azure_resources.csv`. (MF3) `src/finops_assess/demo/azure_resources.csv` re-copied byte-for-byte from `samples/` (958 bytes both sides). (MF4) **the big one** -- Diego's `tests/test_az_commitment_under_covered.py` was a literal duplicate of `tests/test_engine.py` (same blob OID `7f39ad5b...`); deleted and rewrote 13 unit + e2e + cross-run tests covering every Maya plan §3.8 case (positive fire, E2/E3/E8/E9/E10 abstain paths, dedup invariant, twice-applied redaction, cleartext path, intentional dual-fire with `AZ.RESERVATION_UNDERUTILIZED`, real `run_rules` engine e2e, cross-run principal instability, and the E11 over-count regression net for Single-scope owner sub). (MF5) Added `# E11` 3-line comment immediately above the `for sibling_sub, on_demand in sibling_spend.items():` loop in `rules_impl/azure_rules.py` so the conservative over-count posture is documented in code, not just in plan §2.2/§2.5. (N1) `samples/azure_resources.csv` rewritten with LF newlines (CR=0 LF=7 verified) and re-copied to demo mirror. (N2) cross-rule isolation comment citation drift handled by switching the commit body to generic prose without line numbers. **Validation gates all green locally:** `finops-assess validate` (87/7/25), `ruff check .`, `ruff format --check .`, `mypy src` strict, `python scripts/generate_docs.py --check`, and `pytest` (530 passed, 1 deselected for the pre-existing WeasyPrint Windows libgobject gap; PDF tests ignored on Windows for the same reason). **Test count delta:** -32 duplicates, +13 unique behavioural tests. **Lockout protocol learning reinforced:** when stage-4 reject fires due to a scaffolding mistake (wrong file content copied), the original implementer's diff stat can mask the gap entirely (+168 LoC looked like real new tests in the GitHub UI); the binding remediation is to rewrite the test file from the plan §3.8 spec directly, not to patch the duplicate. **Going forward:** per user direction this session, Yuki is now default implementer for #59 rules 3/4/5; Diego steps back to research/quality role. Capability follows work; Yuki remains on Opus 4.7 xhigh for stage-5 implementations.
