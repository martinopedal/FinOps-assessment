# Project Context

- **Project:** FinOps-assessment
- **Created:** 2026-05-04

## Core Context

Agent security-reviewer initialized for the FinOps-assessment squad. See `.squad/agents/security-reviewer/charter.md` for role, boundaries, and voice.

## Recent Updates

 Squad team expanded on 2026-05-04 to cover the M0–M7 roadmap (M365, Azure, GitHub/ADO surfaces + security review + test ownership).

## Learnings

- 2026-05-12  ,  Stage-4 pass on Maya's gap analysis: PR #22 (FOCUS 1.2 mapping) is read-only-posture-clean against hard rules 1–5; only residual risk is *expectation drift* from the "Source field" column, mitigated by a non-contract banner. The repo's docs-only roadmap PRs are a robust pattern  ,  they let us reason about future contracts without committing schema or auth surface.
- Routing-table drift (`milestone:M1..M7` → `release:*`) is pure stale documentation: the labels never existed, no historical link expects them, no external reference relies on them. Hard replace, no redirect  ,  confirmed by `gh label list`, `git log -p`, and a repo-wide `rg "milestone:M"`.
- Biggest second-order risk I flagged for the backlog: D.4–D.7 spike issues should pre-commit the eventual implementer to OIDC-only auth in the issue body, so hard rule 2 is not re-litigated in the collector PR. And sovereign-cloud / tenant-id leakage is *not* solved by default PII redaction  ,  keep that distinction sharp when M365 SKU-mix collector work eventually lands.
- Bias check passed: I set a falsification criterion ("stage-4 produces zero amendments") that specifically indicts my own role, and the rollback path preserves my function as a review rubric rather than a workflow gate.
- 2026-05-12T08:26Z  ,  Squad-cli upstream audit (issue #26, Sam): Local v0.8.25 vs upstream v0.9.4; no posture-relevant drift (no auth changes, no permission changes, no PII implications). Intentional local customization justified. No re-review needed.
- **2026-05-12T08:42Z  ,  Noor Learning: Hard-rules enforcement moves to PR review.** The stage-4 adversarial role is reframed as a *rubric voice* in the PR body's self-review section (for @copilot-direct work) rather than a separate formal spawn for every frontier epic. The five hard rules (read-only posture, no secrets, no copyrighted redistribution, PII redaction, catalogue-as-data) remain non-negotiable  ,  they are now enforced at PR review time rather than as dedicated stage-4 spawns for D.4–D.7 work. On-request spawns for genuinely non-trivial PRs (e.g., architecture proposals, security audits, cross-system design) remain available and continue to exercise the full stage-4 adversarial pass. This is a shift in *ceremony*, not a shift in *posture*  ,  the hard rules are as load-bearing as ever; they are just checked at a different point in the workflow.

- **2026-05-13  ,  Noor Learning: Stage-4 on PR #38 (chore(squad) routing-enforcement refusal rule, Sam, closes #35).** Verdict: ✅ approve (submitted as comment per Copilot-self-approval block). The PR is the mechanical follow-up to Sam's own squad-cli audit (issue #26 / 2026-05-12 decision): cherry-pick the routing-enforcement clause from upstream PR #890 *without* the wholesale v0.9.4 upgrade. Diff is two files (governance + Sam's history), strictly additive, voice-matched (bold prefix + second-person imperative  ,  same shape as items 1–5), and the version stamp `0.8.25 → 0.8.25+local` is the right convention for a cherry-pick (honest divergence marker, not a fake-bump). Closest-to-triggering hard rule was Rule 3 (no third-party copy): Sam paraphrased rather than lifted (upstream item #3 attacks "never skip dispatching"; Sam's item #6 attacks the *rationalization* "the answer feels small") and attributed upstream PR #890 in the commit message  ,  clean. Pattern for future squad-cli cherry-picks: (a) keep the diff to one substantive file + agent-history hygiene, (b) use `+local` version-stamp convention to flag cherry-pick vs wholesale, (c) confirm voice-match against existing list items in the same section before merge, (d) verify the new clause is more-restrictive (additive) rather than a replacement  ,  replacements need a separate decision entry.

- **2026-05-13  ,  Noor Learning: Stage-4 on PR #40 (epic(roadmap) M365 SKU-mix family-summary contract, Priya, closes #29).** Verdict: ✅ approve (submitted as comment per Copilot-self-approval block). The high-risk axis I flagged in the original gap analysis  ,  GSA / sovereign-tenant PII leakage being orthogonal to default user-level PII redaction  ,  is addressed at the **schema layer**, not in a promise. `M365FamilySummary` has `ConfigDict(extra="forbid")` and two distinct ValidationError tests (`test_m365_family_summary_rejects_extra_fields` covers `tenant_id`, `test_m365_family_summary_rejects_per_principal_fields` covers `user_id`) prove rejection rather than asserting it in a docstring. Belt-and-braces: the open `feature_usage_signals: dict[str, int]` escape hatch is closed by a third test asserting `{"users": [...]}` is rejected (type-level, since values must be `int`). The only GUID in the PR is the documented null GUID `00000000-0000-0000-0000-000000000000` used as a rejection-test fixture  ,  not a real tenant ID, hard rule 2 holds. Pattern for future schema-contract PRs in PII-sensitive surfaces: (a) the `extra="forbid"` claim must be paired with an explicit ValidationError test on the *exact* shapes you're forbidding  ,  `tenant_id`, `subscription_id`, `principal_id`, `user_id`, `upn`  ,  not a generic "extra fields rejected" assertion; (b) any open `dict[str, X]` field needs a parallel test proving the value-type constraint can't be used to smuggle a user list; (c) `Literal[...]` enums baked into pydantic models are *contract bounds* and do NOT conflict with catalogue-as-data  ,  the distinction is "schema-level abstraction names" (fine in code) vs "SKU/price/feature catalog" (must live in YAML). Priya got this right by using internal family-name abstractions (`m365_e5_tier`, `defender_o365_p1`) rather than vendor SKU IDs (`SPE_E5`); the collector will do the catalog→family mapping. Hard rule 3 cleanly handled  ,  M365 Maps linked + paraphrased, no diagrams copied; could be slightly louder in the model docstring itself but the link-plus-attribution pattern is sound. Squad-rubric reframe (2026-05-12 decision) is holding: this was an on-request stage-4 spawn for a genuinely non-trivial schema PR, exactly the carve-out the decision preserved  ,  not every PR needs ceremony, but contract PRs that touch the PII boundary still do.

- **2026-05-13  ,  Noor Learning: Bakeoff verdict on issue #27 (Azure pricing observation contract)  ,  PR #39 (Diego) wins, PR #36 (bot) superseded.** Side-by-side review under the falsification framing (2026-05-12 *Squad reframed as review rubric* decision). Both PRs pass all five hard rules; the call comes down to acceptance-criteria substance and pattern-setting for #28 (RI/SP) and #30 (agreement-types). #39 has substantive §11 stages 1–3 in the body (an explicit acceptance criterion that #36 only gestures at), 19 dedicated tests vs #36's 2 new + 1 extended, an observation-vs-catalog boundary made unambiguous via its own `src/finops_assess/pricing.py` module, and explicit open-questions documentation for #28/#30 plus a decision-inbox entry. #36 is competent and end-to-end-wired (CSV collector + `NormalizedDataset` + `docs/schema.md`), but co-locates pricing observations with customer-state observations in `models.py` and inside `NormalizedDataset`, which quietly invites #28 and #30 to do the same and erodes the boundary issue #27 asked us to draw sharply. **Implication for the falsification log:** this is one data point *for* the value of the full §11 ceremony on schema-setting frontier-epic work  ,  Diego's PR has a design intent and pattern-setting artefact (the open-questions section + inbox entry) that the autonomous bot attempt did not produce, and that artefact is exactly the kind of thing that would have been missed by a single-author `@copilot`-direct pass. It does **not** undermine the rubric-reframe decision in the general case (the 22 prior `@copilot`-direct PRs still shipped fine without ceremony), but it does support the carve-out in that decision: *on-request multi-agent spawns remain available for genuinely non-trivial PRs (architecture proposals, security audits, frontier-epic kickoffs)*. Schema-setting on a frontier epic that *sets the pattern* for two follow-on epics is exactly that carve-out. Pattern for future bakeoffs: when the acceptance criteria explicitly include §11 stages 1–3 in the PR body, the bot's structural-claim-without-substance is a near-deterministic loss against a full-ceremony attempt  ,  score that axis before letting code-quality axes dominate.

- **2026-05-13  ,  Noor Learning: Stage-4 on PR #41 (feature(reporter) FinOps practice-review section advisory, Maya, closes #31).** Verdict: ✅ approve (submitted as comment per Copilot-self-approval block). The headline risk on this PR  ,  the advisory-vs-certification boundary  ,  is closed at the **test layer**, not in copy review: `test_no_certification_language_in_section_body` calls `render_practice_review_section(report)` against a fully-populated fixture (so all four sub-sections render with actual content), strips only the single `<p class="disclaimer">…</p>` paragraph via non-greedy regex, and asserts `Level / Score / Certified / Certification / Rating` are absent from the rendered HTML body. That is a scan of the *output*, not the disclaimer string  ,  a future copy revision that smuggles "Level 2" into the SKU-mix paragraph fails this test. Belt-and-braces: a second test (`test_disclaimer_contains_no_forbidden_certification_words`) holds the disclaimer constant itself to the same bar, closing the smuggle-via-disclaimer vector. Pattern that worked for Maya: **strip-then-scan over the rendered output**, not assert-on-source-strings  ,  this is the right shape for any future advisory section. Derived-view discipline held cleanly: nine files, zero edits to `models.py` / `pricing.py` / `json_reporter.py`, defensive reads (`isinstance(payload, dict) or not payload`) for both not-yet-surfaced upstream contracts (#28 commitment coverage, #29 family summaries). Graceful-degradation tests use *absent* keys, not present-but-empty  ,  that's the harder case and matches the actual not-yet-merged upstream shape. One non-blocking suggestion forwarded: the forbidden list could plausibly grow to `Maturity / Tier / Grade / Index`, but note the disclaimer deliberately uses "maturity assessment" and "grade", so any expansion needs the same disclaimer-strip carve-out  ,  file as follow-up if/when copy is revised, not now. Pattern for future derived-view PRs over the canonical report: (a) the renderer must read `report[...]` defensively with `isinstance` guards on every optional sub-shape, (b) the forbidden-word guard must run against `render_*(report)` output with a deterministic strip of any negation-paragraph, not against the source strings, (c) "graceful degradation" tests must use the *absent-key* shape (not present-but-empty), since that's the actual shape of not-yet-merged upstream contracts, (d) every derived-view PR should regenerate the committed example artefact in the same commit so reviewers can eyeball the rendered output. Hard rules 1–5 all hold cleanly: pure dict→string transform (rule 1), no GUIDs or secrets in fixtures (rule 2), original disclaimer copy with operator-facing posture cues not vendor-brochure paraphrase (rule 3), no principal-ID re-derivation in posture content plus a defensive warning when `pii_redaction == "disabled"` (rule 4), family names rendered verbatim from input data with zero hardcoded SKU strings (rule 5).

- **2026-05-13  ,  Noor Learning: Stage-4 on PR #37 (docs(skills) Run assessment runbook, Yuki).** Verdict: ✅ approve (submitted as comment  ,  GitHub blocks self-approve on @copilot-authored PRs; verdict line is at the top of the body). No material concerns against any of the five hard rules; the runbook is read-only by construction, redaction-default is the headline of Step 5, `(planned)` is consistent, and the §11 stage-4 self-review in the PR body cites all five rules by number. Closest-to-triggering rule was Rule 4 only because the Copilot-helper line (`does not upload data off your machine without explicit flag`) is softer than the rest of the doc  ,  fine for a planned/optional helper, but worth tightening when the helper graduates. Pattern for Yuki on future docs PRs: the section order (Prerequisites → Steps → Planned → Read-only posture → See Also) is now the squad convention for skill runbooks; reuse it verbatim for the next conversion (Prepare CSV inputs) so stage-4 can scan posture in one place. One cosmetic drift to watch: PR-body §4 claimed `5 ❌` bullets in the posture block but the shipped file has 4  ,  keep self-review counts in sync with the file.

- **2026-05-13  ,  Noor Learning: Stage-4 on PR #42 (epic(roadmap) Azure RI/SP commitment data model, Diego, closes #28).** Verdict: **REQUEST CHANGES** (submitted as comment). Schema substance and posture are clean against all five hard rules  ,  Diego correctly extended `src/finops_assess/pricing.py` rather than inventing a new module, preserved the observation-vs-catalog boundary from #27, kept `NormalizedDataset` wiring deferred (consistent with his own #27 decision), and chose `[0, 100]` percentage bounds matching the existing `AzureReservation.utilization_pct` convention. Two must-fixes blocked merge: (1) **scope hygiene**  ,  six §11 stage artefacts (`pr-body.md`, `stage-1..5-*.md`, ~600 lines) committed to repo root despite being already in the PR body; surgical scope is `pricing.py + test_pricing.py + agent history`, the rest is cruft; (2) **language-guardrail test is partial**  ,  Diego's `\b` regex would catch a planted `purchase` in the two class docstrings it scans (verified by mental insertion), but the test does NOT scan `AzureCommitmentDataset.__doc__`, does NOT iterate `model_fields[*].description` (the Field-description string literals where future drift is most likely), and does NOT scan `Literal[...]` enum values; the inline `"purchased"` post-filter is dead code because `\b` already excludes `"purchased"` via the trailing word boundary on `d`. Lesson for future schema-PR guardrail tests: **scan every string surface a rule template or finding renderer could consume**  ,  class docstrings AND `Field` descriptions (via `Model.model_fields`) AND `Literal` enum values AND wrapper-dataset docstrings  ,  not just the two headline class docstrings. The `\b...\b` regex is the right primitive; the gap is coverage, not pattern. Two should-fix nits: missing inclusive-boundary assertions at exactly `0.0` and `100.0` for `utilization_pct` / `coverage_pct` (off-by-one surface), and the prohibited-verb list `["purchase", "buy", "exchange", "modify"]` is unanchored to issue #28 in code so a future maintainer adding `"remove"`/`"delete"` has no rationale to cite. Decision-inbox check unverifiable from the PR  ,  `inbox/` is gitignored per the Governance section in `.squad/decisions.md`, so the commit-message claim of `Created .squad/decisions/inbox/diego-pricing-observation-contract.md` cannot be cross-checked; flagged as process limitation, not defect. Pattern for future observation-contract PRs in this module: (a) the §11 self-review section in the PR body is the right home for stages 1–5, **not** repo-root scratch files  ,  explicitly call this out in the PR template or contributor docs; (b) any language-guardrail test must enumerate every `BaseModel` subclass in the module and walk `model_fields` programmatically rather than hardcoding two class names  ,  that keeps the guardrail honest as the module grows; (c) inclusive `ge`/`le` bounds deserve `0.0` and `100.0` acceptance assertions, not just out-of-range rejection; (d) when extending a pattern from a prior PR, explicitly note in the PR body which decisions from the prior PR are being preserved (Diego did this well for `NormalizedDataset` deferral and the observation-vs-catalog boundary  ,  exemplary).


- **2026-05-13  ,  Noor Learning: Stage-4 re-review on PR #42 lockout-revision (Yuki, 9c6869e + a2ab30a).** Verdict: **REQUEST CHANGES** (submitted as comment  ,  GraphQL blocks self-`--request-changes`). Finding #1 (scratch file cleanup) closed cleanly: `9c6869e` is six pure deletions of `pr-body.md` + `stage-1..5-*.md`, zero collateral, no re-additions in `a2ab30a` or `4d563e6`. Finding #2 (test coverage expansion) is **shape-correct but has a material gap**: Yuki's reflective pattern is actually *stronger* than the shape I originally asked for in three ways  ,  recursive `_extract_literal_values` walks `Annotated`/`Optional`/`Union` to reach inner `Literal`s (I'd only asked for direct Literal scan), failure messages name `model + surface` (e.g. `AzureCommitmentObservation.action.Literal`) for fast diagnosis, and the model list explicitly includes `AzureCommitmentDataset` plus uses `model_fields` programmatically rather than hardcoded field names. But the prohibited-verb regex itself is **weaker** on the exact surface the Literal walk was added to cover: `r"\bpurchase\b"` / `r"\bbuy\b"` / `r"\bexchange\b"` / `r"\bmodify\b"` do not fire against snake_case Literal values like `"exchange_recommended"` / `"purchase_now"` / `"buy_more"` because Python regex `\w` includes `_`, so the trailing `\b` between letter and underscore is not a boundary. Verified empirically: `re.findall(r'\bexchange\b', 'exchange_recommended', re.IGNORECASE) == []`. Mental-insertion trace: docstring `"please purchase the SP renewal"` ✅ caught, Field description `"BUY"` ✅ caught (case-insensitive), Literal `"exchange_recommended"` ❌ silently missed  ,  which is precisely the regression class the Literal extraction was added to prevent. Must-fix is small: replace trailing `\b` with `(?![a-zA-Z])` or normalize `_`→` ` on each Literal value before scanning, plus one assertion that the checker raises on the synthetic string `"exchange_recommended"` so this can't silently regress again. Pattern for future guardrail tests in this codebase: **when the scanner is extended to a new surface, the regex anchor must be re-validated against the conventional shape of that surface**  ,  class docstrings and Field descriptions are prose (whitespace-bounded), but Literal enum values are identifiers (typically snake_case), and `\b` is asymmetric across `_`. Cleanup vs Yuki's shape vs my original ask: deletions equivalent, reflective Literal walk stronger than my ask, anchor regression introduced by the IGNORECASE shift to a Literal surface. Lockout-revision discipline otherwise held: `a2ab30a` touches only `tests/test_pricing.py`, no drift into `pricing.py`, surgical scope.

- **2026-05-13  ,  Noor Learning: Stage-4 round-3 spot-check on PR #42 regex-fix (Diego, a5c0297).** Verdict: ✅ **APPROVE (round 3, final)**  ,  submitted as comment. Diego replaced the four `\b...\b` patterns with a single explicit-alphabetic-lookaround alternation `r"(?<![a-zA-Z])(purchase|buy|exchange|modify)(?![a-zA-Z])"` under `re.IGNORECASE`, switched the consumer to `match.group(1)` for the capturing group, and added 3 positive controls (`exchange_recommended`, `purchase_now`, `BUY_SP`) + 1 negative control (`backup`). Mental-insertion traces all clean: `modify_now` matches (space/`_` boundaries), `baby_buy_signal` matches (`_`/`_` boundaries), `upbuy_signal` correctly rejected (`p` is alphabetic, lookbehind fails), `backup` rejected (no token substring with non-alpha boundary). Reflective `_extract_literal_values` walk and `model_fields` iteration preserved verbatim  ,  diff is scoped solely to `tests/test_pricing.py` (24+/6-), zero drift into `pricing.py`. **Reusable rule of thumb (the headline learning from this 3-round arc):** Python's `\b` is asymmetric across `_` because `\w` includes underscore, so `\bword\b` silently fails on the `word_suffix` / `prefix_word` shape that dominates `Literal[...]` enum values and any other identifier-style surface. **For snake_case identifier guards, never use `\b`  ,  use either explicit alphabetic lookaround `(?<![a-zA-Z])(...)(?![a-zA-Z])` or pre-normalize `_`→` ` on the input before scanning.** Prose surfaces (docstrings, Field descriptions) tolerate `\b` fine; identifier surfaces (Literal values, enum members, field names if ever scanned) do not. When a guardrail scanner is extended to a new surface, re-validate the anchor against the conventional lexical shape of that surface  ,  round-2 regression was anchor-on-prose applied to anchor-on-identifier without re-validation. 3-strikes escalation **not triggered**: round-1 was reflective-coverage (Yuki shipped, accepted), round-2 was anchor-on-identifier gap (Diego closed cleanly), round-3 is verify-only. Pattern for future: positive-control assertions on the exact regression-class string (here: `exchange_recommended`) are cheap insurance against silent re-introduction  ,  make this the squad default for any future guardrail-test PR.

- **2026-05-13  ,  Noor Learning: Stage-4 on PR #43 (epic(roadmap) Agreement-types pricing-profile contract, Diego, closes #30)  ,  third and final pricing-chain extension.** Verdict: ✅ approve. Closes the #27→#28→#30 pricing-contract trilogy. The two high-risk axes called out by issue #30 acceptance  ,  **copyright** and **tenant-rate-card hardcoding**  ,  both land clean: Microsoft Learn links only (no EA/MCA/CSP discount-tier matrix or pricing diagrams bundled, range characterizations like "EA typically 0.80–0.95" are narrative paraphrase not table redistribution), and discount_multiplier defaults to 1.0 with source: Literal["default_list", "customer_supplied"] enforcing the no-rate-card-in-repo posture at the schema layer. Pattern across the pricing-chain (worth carrying forward): (a) **default-to-list posture must be enforced at the schema default**, not in a docstring promise  ,  Field(default=1.0) + Field(default="default_list") is the right shape, and the explicit-defaults test (	est_pricing_profile_defaults) proves it; (b) **source provenance field is the load-bearing primitive** for distinguishing "no customer data" from "customer explicitly provided"  ,  without it, rules downstream cannot adjust confidence scores or skip pairings, and the no-rate-card-in-repo claim becomes unfalsifiable at the rule layer; (c) **synthetic discount fixtures must be deliberately unrealistic** (0.85, 0.92) so they cannot be back-traced to any specific Microsoft published tier  ,  this is the copyright-paraphrase test applied to numeric data; (d) **currency posture stays Literal["USD"] until rules ship cross-currency handling**  ,  declining auto-conversion at the model layer is the correct conservative call (exchange rates are temporal, multiplier application would be undefined); (e) **language guardrail with explicit-list-of-models is fine if every author remembers to append**  ,  Diego correctly extended the commitment_models list to include PricingProfile, but his PR-body framing of "reflective auto-discovery" was imprecise. For the next pricing-extension author, flag explicitly that the guardrail is opt-in-by-append, not opt-in-by-import  ,  otherwise someone will assume auto-discovery and skip the manual extend. One cosmetic off-by-one in Diego's "11 new tests" count (10 new + 1 modified-existing); functionally 42 tests collected, all pass, ruff+mypy clean. Across the three pricing PRs the §11-stages-in-body convention held, co-authored-by trailers held, surgical-scope held, and the squad-state hygiene lesson from #28 round-1 (no scratch files committed) held all the way through. The pricing-chain is now a reusable template for any future observation-family addition: separate module (pricing.py), xtra="forbid", ISO 8601 date constraints, source provenance Literal, currency: Literal["USD"] baseline, language-guardrail extension, and an open-questions / decision-inbox artefact to bridge to the next epic.

- **2026-05-12T -- Noor Learning: Stage-4 on PR #52 (ci: add required-checks summary job, Maya, closes #51).** Verdict: ✅ approve. The required-checks pattern is the canonical GitHub Actions idiom for aggregating matrix/shard status into a single stable check context. Noor's stage-4 review confirmed: (1) the `if: always()` + `needs.*.result !== 'success'` assertion is the right shape for handling failure/cancelled/skipped; (2) the forward gotcha (binding on future Stage-4 reviewers)  ,  every new top-level job added to `ci.yml` must be appended to the summary's `needs:` list  ,  is clearly documented and non-negotiable. The design choice to publish a deliberate aggregator job (`required-checks`) rather than trying to reference a workflow name or matrix dimension is sound and decouples the protection contract from CI/CD matrix changes. Bootstrap cost acknowledged: the PR itself still requires one final `enforce_admins` toggle-dance (Coordinator's responsibility post-merge). **Critical validation:** the `required-checks` pattern composes with the auto-approve workflow (#47/#48) to enable fully-async squad PR merges: open → squad label → Noor verdict comment → bot approval → all-green CI (including the new summary) → `gh pr merge --squash` with zero manual toggles. If this PR merges without `--admin` and the subsequent Coordinator protection PATCH lands cleanly, the toggle-dance era is over and the inaugural test of the new async path is a success.


- **2026-05-13 — Noor Learning: Stage-4 adversarial review for issue #58** (FOCUS-aligned advisory exporter) spawned in parallel with Maya's stage-3 plan. Both running on Opus 4.7; no blocker dependencies. Plan delivery pending Noor's verdict comment; auto-approve workflow will fire on verdict:APPROVE + stage-4-marker tags per .squad/decisions.md 2026-05-12 entry.
- **2026-05-13 — Noor Learning: Stage-4 APPROVE on issue #58 (Maya, FOCUS-aligned advisory exporter v0.5.0).** Verdict: ✅ APPROVE; verdict at .squad/decisions/inbox/noor-stage4-58.md. Three design-choice notes worth keeping for future export-reporter / sidecar-manifest PRs: (1) **Rule.evidence_key_version: int = 1 shipped-but-unused posture is sound** when paired with a manifest field that documents the algorithm (vidence_key_algorithm) and a v0.6.0 escape valve (manifest_schema_version bump to `"0.2"` when the version is mixed into the hash). The cost is one optional pydantic field with a default; the value is a versioned join-key migration contract that consumers can rely on. Cheaper than retrofitting after warehouses have indexed on the key. (2) **Manifest additive-evolution discipline has a JSON-Schema wart**: adding KEYS is consumer-strict-additive, but extending ENUM VALUES on existing keys (e.g. v0.6.0 expanding pii_handling.mode from {"azure_resource_id_cleartext"} to {...,"stable_salt","ephemeral_salt","cleartext"}) breaks v0.5.0 strict validators. The "consumers MUST ignore unknown fields" guarantee in docs/focus-export.md does NOT cover this. Future plans that walk a manifest-schema-version lifecycle should embed an explicit description on enum fields signalling "value set will expand under additive bumps; widen your accepted set". P2, not blocking — industry-standard JSON-Schema-evolution wart, not Maya's invention. (3) **ARM-resource-ID-as-cleartext PII tail risk** is correctly handled at the manifest layer (pii_handling.mode = "azure_resource_id_cleartext" is an explicit operator signal) and downstream-echo layer (source_report.pii_redaction mirrors the upstream redaction flag). The tail risk is real: resource names CAN encode user/customer names (m-john-test01), but hashing the ResourceId would defeat the join-to-FOCUS-warehouse purpose of the export. The right answer is operator-facing transparency via the manifest mode field, NOT exporter-side hashing. **Pattern for future export-reporter PRs:** any sidecar-manifest design must walk through (a) the v+1 additive-evolution case explicitly (which fields/enums extend, what consumers see), (b) the PII-cleartext rationale ANCHORED to a downstream-join requirement (not "we forgot to hash it"), and (c) the docs-freshness gate (scripts/generate_docs.py --check) coverage of any committed example artefact. Maya's plan hit all three. Stage-4 carve-out from the 2026-05-12 reframe held: this was an on-request multi-agent spawn for a frontier-epic PR (cross-PR contract surface, JSON Schema sidecar, evidence-key migration mechanism) — exactly the case the rubric-reframe decision preserved ceremony for.
- **2026-05-13 — Noor Learning: Stage-4 adversarial review for issue #61 (playbook / ticket reporter, v0.5.0) spawned in background.** Both Maya (stage-3 plan author) and Noor (stage-4 adversarial reviewer) running on Opus 4.7 xhigh. Plan committed to `.squad/decisions.md:6-500` (150KB decisions file, all divergences D1–D4 locked, research OQs OQ1–5 closed, Noor predictions N1–N5 pre-empted). Maya identified one stage-3 correction vs consensus (D2 Option B-honest on per-run salt, verified against repo at commit 39b3230). Two follow-ups filed: #73 (stable-salt engine, v0.6.0 p1), #74 (runtime overlay sandbox, v0.6.0 p2). Placeholders resolved (commit 2613b03). PR #72 open (draft, squad:maya + release:v0.5.0). Noor verdict pending — do not log until verdict comment lands.
- **2026-05-13 08:55Z — Noor Learning: Stage-4 VERDICT on issue #61 — REJECT (2 BLOCKING / 8 AMENDMENT / 3 NIT).** Verdict comment: PR #72 comment 4438990637. Two blocking findings at the architecture-level: **B1 (manifest-JSONL pair-atomicity).** Plan §5.1 writes JSONL via `tempfile.mkstemp()+os.replace()`, *then* builds manifest. Crash between two `os.replace` calls leaves byte-complete JSONL on disk with no manifest; operator cannot detect partial output and pipes stale rows into ServiceNow with no per-surface stability declaration. Mitigation: write manifest *before* JSONL `os.replace`, or stage both temps + atomic pair-replace. **B2 (.j2 template LF-pinning regression).** Plan §1 N5 / file #22 lists `examples/` golden artefacts + `tests/fixtures/playbook/` golden bytes, but 23 `.j2` source templates under `src/finops_assess/data/playbooks/{surface}/` are the *input* to every render. On Windows clone with `core.autocrlf=true`, templates become CRLF; Jinja2 renders `\r` in title/description; `json.dumps` escapes to `\\r`; `test_golden_jsonl_byte_identical` fails on windows-latest runners while passing on Linux. **Exact regression of #58 hardening** (Yuki's commit 3e18275 pinned golden fixtures to LF in `.gitattributes` under `examples/` and `tests/fixtures/focus_aligned/`). The pattern should have been applied preventively to `src/finops_assess/data/` files that are direct inputs to renders, not just to output fixtures. 8 amendments on evidence-tracking, test structure, adapter-hints semantics, manifest stability declaration, CLI warning conditions, docs outline, sort stability, duplicate removal. 3 NITs on test promotion, schema versioning, JSON-escaping caveats. **Pattern learning (binding for future plan reviews):** hardening-precedent walk-through is a critical stage-3 planning artifact. When a plan enters scope similar to a recent PR's hardening fixes, stage-3 must explicitly ask "which patterns from recent PRs apply here?" and cite the carry-forward or explain why it doesn't apply. The #58 LF-pin pattern was a regression *only because* Maya's planning checklist did not prompt this cross-referential check. Recommended stage-3 checklist addition: "§ Hardening precedents: does this scope overlap recent hardened PRs? If yes, explicitly carry forward or document why not applicable." **Reviewer Rejection Lockout triggered.** Per Maya's own §10 (lines 585–588) in her stage-3 plan, Maya is **locked out** of revising her own rejected plan. Revision routed to Yuki (QA/hardening specialist, eligible as non-original-author, Opus 4.7 xhigh exception for stage-3 planning work). Yuki to address all 13 findings without tampering with locked architectural decisions (D1/D2/D3/D4, OQ-1..5, §9 deferred-disposition). Verdict orchest-log: `.squad/orchestration-log/2026-05-13T085500Z-noor-stage4-61.md`. Noor re-review spawned post-Yuki revision (verdict pending).
- **2026-05-13 — Noor Learning: Stage-4 RE-REVIEW scheduled (in flight).** Yuki's revision appended to `.squad/decisions.md:6-1060` under new `### Stage-3 Plan Revision (Yuki, post-Noor stage-4 reject)` subsection. All 13 findings addressed (100% closure). **Atomic-write Option C precedent** established: manifest-as-readiness-marker + `os.fsync` durability + `output_artifacts.jsonl_sha256` self-attestation + orphan pre-flight + `--cleanup-orphans` recovery flag. `.gitattributes` LF-pinning applied (commit 10096cb). Re-review verdict not yet logged per protocol (this batch documents spawn, not re-review outcome).

- **2026-05-13T20:30Z  ,  Noor stage-4 on PR #78 (Diego stage-5 implementation of #61 playbook reporter, head `eef9b10`).** Verdict: **REJECT**, 1 BLOCKING + 3 AMENDMENT + 2 NIT. The mandate flagged probe `does the engine's per-run salt leak into a ticket_key declared stable?` triggered: `build_playbook_manifest` hardcodes `azure: stable` regardless of `pii_redaction`, but `RuleContext.redact()` (engine.py:70-75) salts every Azure principal with `secrets.token_hex(16)` per-run when `redact_pii=True` (the default), and every `rules_impl/azure_rules.py` finding emits `principal=ctx.redact(resource.resource_id)`. Empirically confirmed: two synthetic runs with different salts produce different Azure ticket_keys while the manifest still claims `stable` and `mode: salted_hash` simultaneously — three statements that cannot all be true. Visible in the shipped demo at `examples/playbook.jsonl.manifest.json` (`pii_redaction: true` + `mode: salted_hash` + `azure: stable`). Diego implemented the locked plan faithfully — the defect is in **the plan I approved**: Yuki's revision (and my consensus pass) both assumed Azure resource IDs were cleartext at the reporter boundary; neither traced through `redact()`. Stage-4 caught what stages 3+4 missed the first cycle. **Lesson for future stage-4 work on PII-boundary PRs: when a plan asserts a per-surface invariant, prove it with a two-run end-to-end fixture (different `secrets.token_hex(16)` values), not a single-fixture spot check that hand-crafts a cleartext principal**. The 230-test playbook suite passes locally because `test_manifest_pii_mode_salted_hash` only asserts the manifest *string claim*, never compares two runs' actual ticket_keys; the absence of that one test is exactly the gap that would have caught the BLOCKING. Pre-existing parallel finding flagged as NIT #2: `focus_aligned.py` (PR #70, Yuki/#58) declares `pii_handling.mode: azure_resource_id_cleartext` based on the same false assumption — file follow-up so both reporters get aligned with the engine reality, ideally as one of three fix paths: (A, smallest) make the stability dict pii-aware in `build_playbook_manifest` (one dict construction, one note rewrite, one new test); (B, cross-cutting, NOT in this PR) bypass `redact()` for Azure surface in the engine — that's #73 (engine tenant-stable PII salt v0.6.0); (C) document the precondition and expose a `--bypass-redaction-for-azure` flag. Recommended: A. Cleanly-passing items recorded so the next reviewer doesn't redo: atomic-write Option C (mkstemp + same-dir + fsync-inside-with + os.replace + manifest-after + on-disk sha256 re-read + tempfile cleanup on exception + `--cleanup-orphans` pre-flight), .j2 LF pinning + exhaustive parametrized regression test, StrictUndefined + pre-compile of every shipped template, `PlaybookTemplateNotFoundError` fail-fast, `evidence_ref`-only in row schema (full evidence dict NOT in the row), `SOURCE_DATE_EPOCH` honored end-to-end with byte-identical-rerun assertions, no scope creep on #73 or #74, schema versioning (`playbook_schema_version: 0.1` locked in code + both schemas, `additionalProperties: true`), no new credential surface (pure dict→files transform), conservative *consider/verify* template wording, no `print()` in library code (CLI uses `click.echo(..., err=True)` for warnings). AMENDMENT items routed to the revision: (#1) `extract_template_vars` re-parses the template AST per render — memoize on rule_id; (#2) `**evidence` spread overrides reserved finding fields like `principal` if a future rule emits an evidence key with that name — namespace under `ctx.evidence` or spread reserved-keys-last; (#3) `_playbook_env.py` docstring claims env is built `at module import` but it's lazy-on-first-call — fix the docstring or call `get_playbook_env()` at module import. Reviewer Rejection Lockout active: Diego cannot produce the revision; route to Yuki (deepest plan-revision context). The fix is surgical (option A) and doesn't relax any read-only / PII / determinism contract.

- **2026-05-13 11:58Z** , Issue #61 / PR #78 stage-4 REJECT verdict + re-review cycle COMPLETE. **Noor Learning: empirical-proof posture for stage-4 cross-run claims.** The BLOCKING #1 verdict was empirically rock-solid (live cross-run test on ef9b10 showed Azure ticket_keys rotating despite manifest saying stable), but the *missing* test (cross-run with real principals via the engine) is exactly what would have caught the false assumption at stage-3. Yuki's revision adds a regression test that runs the engine twice with both redaction states and asserts the manifest claim. **Rule for stage-4 verdicts on per-surface invariants:** A single-fixture spot check is not enough. Require a two-run end-to-end test that uses the real producer (engine, collector, reporter), not a hand-crafted mock. When the plan says "Azure ticket_keys are stable", demand a test that runs the full pipeline twice and proves it or disproves it. This becomes a binding regression test in the implementation. When the plan says "M365 findings are per_run when redaction is on", same bar: two-run end-to-end test with a real M365 rule, not a prose assertion. This pattern would have caught BLOCKING #1 at stage-3 review, not stage-4 empirical review. **Precedent now binding for stage-4 reviewers:** if you flag a plan-level assumption about manifest fields, demand the two-run regression test be added to stage-3's test plan explicitly, called out by name, with both input states enumerated. Yuki's 	est_playbook_cross_run_stability.py is the model. Full verdict at .squad/decisions.md §11 Stage-4 Adversarial Review + Stage-5 note on regression tests and false-assumption pattern learning. Re-review on PR #78 complete; verdict APPROVED (BLOCKING #1 fix vetted, cross-run test binding, focus-aligned mirror applied).
- **2026-05-13 — Noor Learning: PR #78 stage-4 RE-REVIEW formal verdict on Yuki's revision `5bf48e8` (under Reviewer Rejection Lockout).** Verdict: APPROVE — comment URL https://github.com/martinopedal/FinOps-assessment/pull/78#issuecomment-4440298763. All six original findings RESOLVED at the implementation level: BLOCKING #1 (pii-aware stability dict in playbook.py:108,468–475 + honest example manifest at examples/playbook.jsonl.manifest.json:18–27), AMEND #1 (@functools.cache _template_vars_cached returning tuple so callers cannot mutate the cached value, playbook.py:339–351), AMEND #2 (**evidence spread first / reserved keys second in render_row, playbook.py:386–399), AMEND #3 (lazy-init docstring honest at _playbook_env.py:1–35), NIT #1 (cross-run regression test tests/test_playbook_cross_run_stability.py empirically verified non-vacuous: I ran it against eef9b10 and it FAILED with the exact 'stable' == 'per_run' assertion error, then ran it against 5bf48e8 and it PASSED — proves the test catches the BLOCKING-#1 defect class, not a tautology against the new code), NIT #2 (focus_aligned mirror at focus_aligned.py:298–350 + schema enum widened to azure_resource_id_per_run_salted_hash). All five hard rules PASS — the revision specifically *strengthens* Hard Rule #4 by closing the manifest-dishonesty gap. CI: 10/10 SUCCESS on the matrix. Two follow-up issues (#81 repo-wide CRLF hygiene to Maya, #82 NIT bundle to Yuki) are appropriately scoped and don't obscure latent defects. **New pattern learning binding for future stage-4 reviewers:** the empirical non-vacuity check (run the regression test against the broken HEAD AND the fixed HEAD) is the correct way to validate a regression-net, not just running it against the fixed code. A passing test on the new code is necessary but insufficient — you also have to prove the test FAILS on the code it's claimed to gate against. This is the cheapest possible fork-the-fix-out-of-the-tree validation and should become the squad default for any future `regression-net for X` PR review. Re-review confidence: HIGH. Reviewer Rejection Lockout cycle on PR #78 closes here; ready for `gh pr merge 78 --squash --delete-branch`.
