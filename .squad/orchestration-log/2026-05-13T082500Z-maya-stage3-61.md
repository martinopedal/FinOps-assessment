# Orchestration Log Entry

### 2026-05-13 08:25:00 UTC — Stage-3 planning for #61 playbook / ticket reporter (Maya, Opus 4.7 xhigh)

| Field | Value |
|-------|-------|
| **Agent routed** | Maya (Lead / FinOps PM) |
| **Why chosen** | Issue #61 (#57 child) stage-3 canonical plan delivery; Lead role owns planning per `.squad/agents/lead/charter.md` |
| **Mode** | background |
| **Why this mode** | Stage-3 plan is a complex 150KB reasoning artefact; Opus 4.7 xhigh required for consensus reconciliation, all-divergences-locked attestation, and pre-emption of adversarial stage-4 positions |
| **Files authorized to read** | `.squad/decisions/inbox/61-consensus.md`; `.squad/agents/lead/charter.md`; stage-1 research brief + stage-2 rubberduck outputs from session state; repo context (docs/plan.md, src/**, tests/**, data/**, examples/**, .squad/**); PR #70 FOCUS-aligned exporter pattern (precedent) |
| **Duration** | ~180 minutes (10800s estimated) |
| **Tool calls** | 200+ (planning, schema validation, consensus walk-through, all 4 divergences, all 5 OQs, cross-pollination note draft) |
| **Branch** | squad/61-playbook-reporter |
| **PR created** | https://github.com/martinopedal/FinOps-assessment/pull/72 (draft, labels squad:maya + release:v0.5.0) |
| **Outcome** | Completed — §11 stage-3 plan committed to `.squad/decisions.md:6-500` at commit 0928074; all 4 divergences locked (D1 both-additively, D2 Option B-honest, D3 both-complementarily, D4 evidence_ref+template_render_inputs); all 5 research OQs closed; all 5 Noor predictions pre-empted; two follow-up issues filed (#73 stable-salt, #74 template-overlay); placeholders resolved in second commit 2613b032 |

---

## Notable Decisions Locked

**D2 (PII stable-ID) — Option B-honest variant:**
- Verified GPT's #61 stage-2 BLOCKING finding correct: `engine.py:70-75,151` per-run salt makes non-Azure principal hashes unstable across CLI invocations
- Playbook reporter emits both `ticket_key` (dedup) and `finding_revision` (evidence-shift detector)
- Manifest declares per-surface stability: `"stable"` for Azure, `"per_run"` for M365/GitHub/ADO when redaction on + no stable salt
- CLI emits stderr warning on applicable condition
- Engine-level stable-salt feature deferred to follow-up issue #73 (not in v0.5.0 scope)

**D1 (Payload model) — Both additively:**
- Core row shape mirrors Sonnet's proposal (Issue #61 scope)
- Optional nested `adapter_hints.{servicenow,jira,github}` object added (GPT's neutral-row position), derived from `severity` + new `Rule.adapter_class` field
- Ship in row schema v1; no forward compatibility burden

**D3 (Jinja2 hardening) — Both complementarily:**
- Pre-compile templates at startup (Sonnet position) + configure `StrictUndefined` (GPT position)
- Single `_load_playbook_environment()` helper builds env with both properties
- Syntax errors caught at export start; missing-variable errors at render time

**D4 (Evidence in row) — evidence_ref + template_render_inputs:**
- Row carries `evidence_ref` (lookup key only, not full evidence dict)
- New `template_render_inputs: list[str]` field records which evidence keys the Jinja2 template actually touched
- Computed via custom finalize hook or post-render diffing; bounded row size, debuggable operator trail

---

## Files Authorized for Stage-5 Implementation

**Diego (implementer, reporter module owner per #58):**
- Full stage-3 plan body in PR #72 description (commit 0928074)
- Consensus document `61-consensus.md` (locked inputs §1)
- All four divergence lockings (§1, D1–D4)
- Schema pinning (§3, row v1 + manifest schemas)
- Code patterns (§4, loading / template env / fail-fast coverage policy)
- Determinism contract (§5.2, SOURCE_DATE_EPOCH, atomic writes)
- All 25 file-level change targets (§6)

**Yuki (test/docs/fixtures collaborator per #58 precedent):**
- .gitattributes checklist for golden fixtures (Yuki's hardening decision from #58)
- Golden-fixture pinning pattern from #58 SKILL.md
- Test pattern (generative + byte-identical comparisons)

---

## Notes

All four divergence points from stage-2 rubberducks reconciled into a single locked plan (no reopening during stage-5). Stage-3 plan honors: (1) Sonnet's schema-versioning + structured adapter hint; (2) GPT's per-surface stability honesty + vendor-ready superset principle; (3) Yuki's hardening discipline (golden fixtures + .gitattributes); (4) Maya's consensus-correction on D2 (Option B defers stable-salt to #73). Follow-ups #73 (stable-salt engine feature) and #74 (runtime template overlay sandbox) are filed separately and do not block v0.5.0. Stage-4 adversarial review (Noor) spawned; verdict pending.
